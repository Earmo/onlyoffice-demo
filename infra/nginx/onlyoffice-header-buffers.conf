# ONLYOFFICE "Download as" 功能把 cmd JSON 和 JWT tokenSession 放在 URL 参数里，
# 导致请求行超过 nginx 默认 8k 单缓冲区限制，触发 413 Request Entity Too Large。
# 此文件内容会被 include 在 http{} block 内（via /etc/nginx/conf.d/*.conf），
# 在 http 层将缓冲区调大，对所有 server 块生效。
client_header_buffer_size  32k;
large_client_header_buffers 8 64k;
