---
phase: 07-module-boundaries-and-repository-refactor
plan: 02
type: data-refactor
wave: 2
depends_on:
  - 01
files_modified:
  - packages/server/onlyoffice-integration-data/src/main/java/**/DocumentMetadataEntity.java
  - packages/server/onlyoffice-integration-data/src/main/java/**/DocumentMetadataMapper.java
  - packages/server/onlyoffice-integration-data/src/main/java/**/DocumentMetadataRepository.java
  - packages/server/onlyoffice-integration-data/src/main/resources/db/migration/V2__rename_document_metadata_columns.sql
  - packages/server/onlyoffice-integration-data/src/test/java/**/DocumentMetadataMapperTest.java
  - packages/server/onlyoffice-integration-data/src/test/java/**/DocumentMetadataRepositoryTest.java
autonomous: true
requirements:
  - DATA-01
  - DATA-02
user_setup: []
must_haves:
  - `DocumentMetadataMapper` 不再保留 `@Select` 自定义 SQL
  - repository 成为文档元数据自定义查询的唯一入口
  - 表字段和实体字段命名同步收敛到 `*_user` / `*_time`
---

# Plan 07-02: 重构数据访问层并统一字段命名

## Outcome

把当前“Mapper 既做 CRUD 又写注解 SQL”的结构改成 repository 驱动的领域查询层，并通过新的 Flyway 迁移完成字段命名收敛。

## Tasks

<task id="7-02-01">
  <read_first>
    <file>packages/server/onlyoffice-integration-data/src/main/java/com/earmo/onlyoffice/integration/data/mapper/DocumentMetadataMapper.java</file>
    <file>packages/server/onlyoffice-integration-data/src/main/java/com/earmo/onlyoffice/integration/data/repository/DocumentMetadataRepository.java</file>
    <file>packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/service/DocumentMetadataService.java</file>
    <file>.planning/phases/07-module-boundaries-and-repository-refactor/07-RESEARCH.md</file>
    <file>.planning/REQUIREMENTS.md</file>
  </read_first>
  <action>
    在 data 模块中新增 `DocumentMetadataRepository`，把 `selectByTenantIdOrderByUpdatedAtDesc(...)` 和 `selectBySourceSystemAndExternalDocumentId(...)` 这两类领域查询迁移到 repository。
    具体目标状态：
    `DocumentMetadataMapper` 只保留 `BaseMapper<DocumentMetadataEntity>` 能力和必要的 `@Mapper` 声明，不再包含任何 `@Select`、`@Update`、`@Delete`、`@Insert` 自定义 SQL；
    `DocumentMetadataRepository` 提供 `listByTenant(String tenantId)` 与 `findBySourceSystemAndExternalDocument(String sourceSystem, String externalDocumentId)`；
    repository 内部优先用 MyBatis-Flex `QueryWrapper` + `selectListByQuery` / `selectOneByQuery` 实现查询，不把 SQL 注解写回 Mapper。
    同步修改 `DocumentMetadataService` 等调用方，让业务层依赖 repository，而不是直接调用带自定义查询职责的 Mapper。
  </action>
  <verify>`cd packages/server && mvn -q -DskipITs -Dtest=*RepositoryTest test`</verify>
  <acceptance_criteria>
    <criterion>`packages/server/**/DocumentMetadataMapper.java` contains `extends BaseMapper<DocumentMetadataEntity>`</criterion>
    <criterion>`packages/server/**/DocumentMetadataMapper.java` does not contain `@Select(`</criterion>
    <criterion>`packages/server/**/DocumentMetadataRepository.java` exists</criterion>
    <criterion>`packages/server/**/DocumentMetadataRepository.java` contains `listByTenant`</criterion>
    <criterion>`packages/server/**/DocumentMetadataRepository.java` contains `findBySourceSystemAndExternalDocument`</criterion>
    <criterion>`packages/server/**/DocumentMetadataRepository.java` contains `QueryWrapper`</criterion>
  </acceptance_criteria>
  <done>自定义查询入口从 Mapper 注解迁移到 repository，数据访问层分工更清晰。</done>
</task>

<task id="7-02-02">
  <read_first>
    <file>packages/server/onlyoffice-integration-data/src/main/resources/db/migration/V1__create_document_metadata.sql</file>
    <file>packages/server/onlyoffice-integration-data/src/main/java/com/earmo/onlyoffice/integration/data/entity/DocumentMetadataEntity.java</file>
    <file>packages/server/onlyoffice-integration-data/target/generated-sources/annotations/com/earmo/onlyoffice/integration/data/entity/table/DocumentMetadataEntityTableDef.java</file>
    <file>.planning/phases/07-module-boundaries-and-repository-refactor/07-RESEARCH.md</file>
  </read_first>
  <action>
    新增 `V2__rename_document_metadata_columns.sql`，显式执行列重命名，而不是回改 `V1`。
    目标列名统一为：
    `owner_user_id -> owner_user`，
    `created_at -> created_time`，
    `updated_at -> updated_time`，
    `last_opened_at -> last_opened_time`，
    `last_callback_at -> last_callback_time`，
    `last_saved_at -> last_saved_time`。
    同步把实体字段改为 `ownerUser`、`createdTime`、`updatedTime`、`lastOpenedTime`、`lastCallbackTime`、`lastSavedTime`，并更新 `@Column(...)` 注解、service 读写代码和测试断言。
    不直接编辑 `target/generated-sources`，而是在修改实体与迁移后通过编译重新生成新的 `TableDef` 常量。
  </action>
  <verify>`cd packages/server && mvn -q -DskipITs -Dtest=DocumentMetadataMapperTest test`</verify>
  <acceptance_criteria>
    <criterion>`packages/server/**/V2__rename_document_metadata_columns.sql` exists</criterion>
    <criterion>`packages/server/**/V2__rename_document_metadata_columns.sql` contains `rename column created_at to created_time`</criterion>
    <criterion>`packages/server/**/V2__rename_document_metadata_columns.sql` contains `rename column owner_user_id to owner_user`</criterion>
    <criterion>`packages/server/**/DocumentMetadataEntity.java` contains `@Column("created_time")`</criterion>
    <criterion>`packages/server/**/DocumentMetadataEntity.java` contains `private Instant createdTime;`</criterion>
    <criterion>`packages/server/**/DocumentMetadataEntity.java` contains `private String ownerUser;`</criterion>
    <criterion>`packages/server/**/target/generated-sources/annotations/**/DocumentMetadataEntityTableDef.java` contains `CREATED_TIME`</criterion>
  </acceptance_criteria>
  <done>数据库 schema、实体字段和 APT 生成常量统一采用新的 user/time 命名。</done>
</task>

<task id="7-02-03">
  <read_first>
    <file>packages/server/onlyoffice-integration-data/src/test/java/com/earmo/onlyoffice/integration/data/repository/DocumentMetadataRepositoryTest.java</file>
    <file>packages/server/onlyoffice-integration-data/src/test/java/com/earmo/onlyoffice/integration/data/mapper/DocumentMetadataMapperTest.java</file>
    <file>packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/service/DocumentMetadataServiceTest.java</file>
    <file>packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/service/DocumentMetadataService.java</file>
    <file>.planning/phases/07-module-boundaries-and-repository-refactor/07-VALIDATION.md</file>
  </read_first>
  <action>
    补齐数据层回归测试，使 repository 和 service 都对新的命名与查询入口有覆盖。
    具体目标：
    新增或重写 `DocumentMetadataRepositoryTest`，断言 `listByTenant("tenant-a")` 能按 `updated_time desc` 返回结果，`findBySourceSystemAndExternalDocument("native", "external-1")` 能返回匹配文档；
    更新 `DocumentMetadataMapperTest` 与 `DocumentMetadataServiceTest`，把旧的 `ownerUserId`、`createdAt`、`updatedAt`、`lastSavedAt` 等断言全部切到 `ownerUser`、`createdTime`、`updatedTime`、`lastSavedTime`；
    确保 service 层不再直接调用已经移除自定义查询职责的 Mapper 方法。
  </action>
  <verify>`cd packages/server && mvn -q -DskipITs -Dtest=DocumentMetadataServiceTest,DocumentMetadataMapperTest,*RepositoryTest test`</verify>
  <acceptance_criteria>
    <criterion>`packages/server/**/DocumentMetadataRepositoryTest.java` exists</criterion>
    <criterion>`packages/server/**/DocumentMetadataRepositoryTest.java` contains `listByTenant`</criterion>
    <criterion>`packages/server/**/DocumentMetadataRepositoryTest.java` contains `findBySourceSystemAndExternalDocument`</criterion>
    <criterion>`packages/server/**/DocumentMetadataServiceTest.java` contains `getOwnerUser()` or `ownerUser`</criterion>
    <criterion>`packages/server/**/DocumentMetadataServiceTest.java` does not contain `getOwnerUserId()`</criterion>
    <criterion>`cd packages/server && mvn -q -DskipITs -Dtest=DocumentMetadataServiceTest,DocumentMetadataMapperTest,*RepositoryTest test` exits 0</criterion>
  </acceptance_criteria>
  <done>repository 和 service 的关键数据访问路径都有自动化回归覆盖，字段重命名不再依赖人工目测。</done>
</task>

## Verification

- `cd packages/server && mvn -q -DskipITs test`
- `rg -n "@Select\\(" packages/server --glob "!**/target/**"`

## Notes for Execution

- repository 中若只需简单筛选和排序，优先选 `QueryWrapper`，不要为了替换注解 SQL 再引入另一套更分散的 XML 查询。
- 新迁移脚本必须可在 PostgreSQL 与测试 H2 PostgreSQL 模式下同时执行。
