---
phase: 06-verification-and-delivery
plan: 02
type: frontend-regression-baseline
wave: 1
depends_on: []
files_modified:
  - packages/web/package.json
  - packages/web/vite.config.*
  - packages/web/src/pages/
  - packages/web/src/components/
  - packages/web/src/router/
  - packages/web/src/test/
autonomous: true
requirements:
  - QUAL-02
user_setup: []
must_haves:
  - 前端拥有正式测试命令，不再只有 build 冒烟
  - 工作台首页与编辑页核心流转具备稳定页面级回归测试
  - ONLYOFFICE 嵌入以 mock 方式测试页面行为，不引入真实 iframe 依赖
---

# Plan 06-02: 为前端列表与编辑流程补测试

## Outcome

为 Vue 官方前端建立正式的页面级自动化回归基线，覆盖工作台首页与编辑页最核心的导航、状态流转和结果展示行为。

## Tasks

<task id="6-02-01">
  <read_first>
    <file>packages/web/package.json</file>
    <file>packages/web/src/main.js</file>
    <file>packages/web/src/router/index.js</file>
    <file>.planning/phases/06-verification-and-delivery/06-CONTEXT.md</file>
  </read_first>
  <action>
    为前端建立正式测试基线。具体要求：
    1. 引入 `Vitest + Vue Test Utils + jsdom` 或等价组合，并在 `packages/web` 提供正式测试命令；
    2. 测试配置要适配当前 Vite + Vue 3 结构；
    3. 如果需要补统一的测试 setup、mock 辅助或渲染 helper，应保持轻量且可复用；
    4. 不引入真实 ONLYOFFICE 嵌入依赖，把测试焦点放在页面行为和状态渲染。
  </action>
  <verify>`cd packages/web && corepack pnpm test -- --run`</verify>
  <acceptance_criteria>
    <criterion>`packages/web/package.json` contains a test script for the frontend test runner</criterion>
    <criterion>Frontend test configuration exists and supports Vue page/component tests</criterion>
    <criterion>`cd packages/web && corepack pnpm test -- --run` exits 0</criterion>
  </acceptance_criteria>
  <done>前端不再只有构建级冒烟，而是拥有正式可运行的自动化测试入口。</done>
</task>

<task id="6-02-02">
  <read_first>
    <file>packages/web/src/pages/DocumentLibraryPage.vue</file>
    <file>packages/web/src/components/library/DocumentCreateActions.vue</file>
    <file>packages/web/src/components/library/DocumentList.vue</file>
    <file>packages/web/src/pages/DocumentEditorPage.vue</file>
    <file>packages/web/src/components/editor/EditorShell.vue</file>
  </read_first>
  <action>
    为工作台首页和编辑页补页面级回归测试。具体要求：
    1. 工作台首页至少覆盖列表加载、空/错状态、创建动作回流或结果高亮中的核心行为；
    2. 编辑页至少覆盖返回列表、切换确认、保存状态区渲染中的核心行为；
    3. ONLYOFFICE 相关请求采用 mock `editor-config` 与 mock API 数据，不追真实 editor 嵌入；
    4. 测试结构要便于后续继续为更多页面行为扩展。
  </action>
  <verify>`cd packages/web && corepack pnpm test -- --run`</verify>
  <acceptance_criteria>
    <criterion>Frontend test files cover `DocumentLibraryPage.vue` or equivalent workbench entry behavior</criterion>
    <criterion>Frontend test files cover `DocumentEditorPage.vue` or `EditorShell.vue` core navigation/state behavior</criterion>
    <criterion>Tests use mocks for editor runtime rather than real ONLYOFFICE embedding</criterion>
  </acceptance_criteria>
  <done>工作台首页与编辑页核心流转已经具备稳定的页面级回归保护。</done>
</task>

<task id="6-02-03">
  <read_first>
    <file>packages/web/src/App.vue</file>
    <file>packages/web/src/style.css</file>
    <file>packages/web/src/router/index.js</file>
    <file>.planning/phases/06-verification-and-delivery/06-VALIDATION.md</file>
  </read_first>
  <action>
    把前端测试基线与现有构建链路收口。具体要求：
    1. 新增测试后，`pnpm build` 仍需保持通过；
    2. 如有必要，整理少量前端结构或测试辅助代码，使页面测试稳定且不与生产逻辑强耦合；
    3. 测试命令、构建命令和文件组织要足够清晰，方便 Phase 6 后续统一验证入口接入；
    4. 不在本计划扩展到 Playwright 或真实端到端 ONLYOFFICE 集成。
  </action>
  <verify>`cd packages/web && corepack pnpm build`</verify>
  <acceptance_criteria>
    <criterion>`cd packages/web && corepack pnpm build` exits 0 after frontend test additions</criterion>
    <criterion>Frontend test setup does not require real ONLYOFFICE runtime services</criterion>
    <criterion>Test-related helpers or setup files are organized clearly under the frontend package</criterion>
  </acceptance_criteria>
  <done>前端测试和构建链路已经能稳定共存，形成可交付的页面级回归基线。</done>
</task>

## Verification

- `cd packages/web && corepack pnpm test -- --run`
- `cd packages/web && corepack pnpm build`

## Notes for Execution

- 本计划的目标是建立稳定页面回归网，而不是模拟真实 ONLYOFFICE 编辑器嵌入。
- 如果页面测试需要 mock router、fetch 或 editor-config，优先做轻量 helper，不要把前端测试基建做重。
