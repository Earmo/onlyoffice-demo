---
phase: 06-verification-and-delivery
plan: 01
type: backend-regression-hardening
wave: 1
depends_on: []
files_modified:
  - packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/web/
  - packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/service/
  - packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/storage/
  - packages/server/pom.xml
autonomous: true
requirements:
  - QUAL-01
user_setup: []
must_haves:
  - 后端高风险链路具备稳定的 controller 与 service 回归测试
  - provider 路由与默认策略具备可扩展的测试骨架
  - Phase 5 的分布式与安全语义不再只靠手工回归
---

# Plan 06-01: 为后端关键路由、存储策略和用户上下文补测试

## Outcome

把后端当前最容易回退的链路收口成稳定回归网：重点覆盖 callback JWT、共享运行状态、ONLYOFFICE 地址模型、远程资源安全边界，以及 provider 路由分发和默认策略行为。

## Tasks

<task id="6-01-01">
  <read_first>
    <file>packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/service/DocumentStatusServiceTest.java</file>
    <file>packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/service/OnlyofficeConfigServiceTest.java</file>
    <file>packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/service/DocumentStorageServiceTest.java</file>
    <file>packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/service/OnlyofficeImageServiceTest.java</file>
    <file>.planning/phases/06-verification-and-delivery/06-CONTEXT.md</file>
  </read_first>
  <action>
    补强后端高风险 service 回归测试。具体要求：
    1. 针对共享运行状态、最近事件摘要、角色化 URL fail-fast、远程资源安全拒绝补更多回归断言；
    2. 测试继续以 service 语义为主，不把仓储层重新拆成大量细粒度测试；
    3. 测试命名和注释要能直接表达“为什么这条链路值得防回退”；
    4. 如果当前测试基线中存在重复 setup，可做小范围测试基建整理，但不能把本计划做成测试重构工程。
  </action>
  <verify>`cd packages/server && mvn -q -DskipITs -Dtest=DocumentStatusServiceTest,OnlyofficeConfigServiceTest,DocumentStorageServiceTest,OnlyofficeImageServiceTest test`</verify>
  <acceptance_criteria>
    <criterion>`packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/service/DocumentStatusServiceTest.java` contains additional shared-state or recent-event coverage</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/service/OnlyofficeConfigServiceTest.java` contains role-based URL or fail-fast regression coverage</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/service/DocumentStorageServiceTest.java` or `OnlyofficeImageServiceTest.java` contains explicit security rejection regression coverage</criterion>
  </acceptance_criteria>
  <done>Phase 5 的高风险 service 链路已经有稳定回归网，不再只靠人工记忆验证。</done>
</task>

<task id="6-01-02">
  <read_first>
    <file>packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/web/DocumentControllerTest.java</file>
    <file>packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/web/DocumentApiControllerTest.java</file>
    <file>packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/web/DocumentController.java</file>
    <file>packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/web/DocumentApiController.java</file>
  </read_first>
  <action>
    补强 controller / API 层回归测试。具体要求：
    1. callback JWT 拒绝、save-status 最近事件结构、editor-config 坏配置语义等高风险 API 行为要有明确断言；
    2. 文档列表、新建、上传、导入和用户上下文错误语义继续保持可回归；
    3. 尽量用现有 MockMvc 结构扩展，不重复造第二套测试入口；
    4. 测试内容应服务交付回归，而不是为了追求数量去覆盖低价值路径。
  </action>
  <verify>`cd packages/server && mvn -q -DskipITs -Dtest=DocumentControllerTest,DocumentApiControllerTest test`</verify>
  <acceptance_criteria>
    <criterion>`packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/web/DocumentControllerTest.java` contains callback rejection or save-status shape coverage</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/web/DocumentApiControllerTest.java` contains list/create/upload/import regression coverage relevant to current delivery baseline</criterion>
    <criterion>MockMvc-based route tests remain the primary API regression entrypoint</criterion>
  </acceptance_criteria>
  <done>交付级 API 行为已经有稳定的 MVC 回归保护。</done>
</task>

<task id="6-01-03">
  <read_first>
    <file>packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/storage/StorageProviderResolverTest.java</file>
    <file>packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/storage/local/LocalDocumentStorageStrategyTest.java</file>
    <file>packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/storage/minio/MinioDocumentStorageStrategyTest.java</file>
    <file>.planning/phases/06-verification-and-delivery/06-RESEARCH.md</file>
  </read_first>
  <action>
    把 provider 路由与扩展点测试骨架稳定下来。具体要求：
    1. 覆盖默认 provider、tenant/sourceSystem 路由优先级和缺省回退语义；
    2. 保持对 `local + minio` 当前基线的回归保护，同时让后续 COS / OSS 能沿同一测试形态扩展；
    3. 如果需要，可补一个更清晰的测试帮助类或数据夹具，但不能引入未实现 provider 的假实现体系；
    4. 全量 `mvn test` 需要继续通过。
  </action>
  <verify>`cd packages/server && mvn -q -DskipITs -Dtest=StorageProviderResolverTest,LocalDocumentStorageStrategyTest,MinioDocumentStorageStrategyTest test`</verify>
  <acceptance_criteria>
    <criterion>`packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/storage/StorageProviderResolverTest.java` contains provider routing priority coverage</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/storage/local/LocalDocumentStorageStrategyTest.java` and `.../minio/MinioDocumentStorageStrategyTest.java` remain green</criterion>
    <criterion>`cd packages/server && mvn test` exits 0 after backend regression additions</criterion>
  </acceptance_criteria>
  <done>当前 provider 基线已经有可扩展的自动化回归骨架，后续新增云存储策略不会从零起测。</done>
</task>

## Verification

- `cd packages/server && mvn -q -DskipITs -Dtest=DocumentStatusServiceTest,OnlyofficeConfigServiceTest,DocumentStorageServiceTest,OnlyofficeImageServiceTest test`
- `cd packages/server && mvn -q -DskipITs -Dtest=DocumentControllerTest,DocumentApiControllerTest test`
- `cd packages/server && mvn -q -DskipITs -Dtest=StorageProviderResolverTest,LocalDocumentStorageStrategyTest,MinioDocumentStorageStrategyTest test`
- `cd packages/server && mvn test`

## Notes for Execution

- 本计划重点是“交付级高风险回归”，不是重写整个后端测试体系。
- repository 层只保留必要覆盖，收益应优先集中到 controller/service 路径。
