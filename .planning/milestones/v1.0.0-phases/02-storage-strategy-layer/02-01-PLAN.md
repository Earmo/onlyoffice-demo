---
phase: 02-storage-strategy-layer
plan: 01
type: backend-storage-contract
wave: 1
depends_on: []
files_modified:
  - packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/config/OnlyofficeIntegrationProperties.java
  - packages/server/onlyoffice-integration-service/src/main/resources/application.yml
  - packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/storage/StorageProvider.java
  - packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/storage/DocumentStorageStrategy.java
  - packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/storage/StorageWriteRequest.java
  - packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/storage/StoredObjectResource.java
  - packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/storage/StorageProviderResolver.java
  - packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/storage/StorageKeyFactory.java
  - packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/storage/local/LocalDocumentStorageStrategy.java
  - packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/service/DocumentStorageService.java
  - packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/config/OnlyofficeIntegrationPropertiesTest.java
  - packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/storage/local/LocalDocumentStorageStrategyTest.java
  - packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/service/DocumentStorageServiceTest.java
autonomous: true
requirements:
  - STOR-01
user_setup: []
must_haves:
  - 文档内容读写不再直接依赖 `DocumentStorageService` 内部的 `Path/Files` 实现细节，而是通过统一存储策略接口完成
  - `storageKey` 统一收敛为 `tenant/sourceSystem/documentId.ext` 结构，并由独立组件生成
  - local 存储从“默认真相源”降级为兼容 provider，但仍可完整跑通 bootstrap、读取、上传、导入和 callback 覆盖写入
---

# Plan 02-01: 抽取统一存储策略接口与领域对象

## Outcome

先把当前本地文件系统实现从业务编排中剥离出来，建立 provider-neutral 的存储合同和 local 兼容实现，为后续接入 MinIO 做准备。

## Tasks

<task id="2-01-01">
  <read_first>
    <file>packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/config/OnlyofficeIntegrationProperties.java</file>
    <file>packages/server/onlyoffice-integration-service/src/main/resources/application.yml</file>
    <file>.planning/phases/02-storage-strategy-layer/02-CONTEXT.md</file>
    <file>.planning/phases/02-storage-strategy-layer/02-RESEARCH.md</file>
  </read_first>
  <action>
    扩展 `OnlyofficeIntegrationProperties`，把当前单一的 `storageRoot` 配置升级为正式存储配置模型。至少新增以下嵌套字段并在 `application.yml` 中补齐默认键：
    `storage.defaultProvider`、
    `storage.routing.sourceSystems`、
    `storage.routing.tenants`、
    `storage.local.root`、
    `storage.minio.endpoint`、
    `storage.minio.bucket`、
    `storage.minio.accessKey`、
    `storage.minio.secretKey`、
    `storage.minio.pathStyleAccess`、
    `storage.minio.secure`。
    同时创建 `StorageProvider` 枚举，枚举值固定为 `LOCAL`、`MINIO`，并新增 `StorageProviderResolver`，其公开方法至少能根据 `RequestContext` 和默认配置返回一个 `StorageProvider`。
    `application.yml` 中显式增加 `ONLYOFFICE_INTEGRATION_STORAGE_DEFAULT_PROVIDER`、`ONLYOFFICE_INTEGRATION_STORAGE_LOCAL_ROOT`、`ONLYOFFICE_INTEGRATION_STORAGE_MINIO_ENDPOINT`、`ONLYOFFICE_INTEGRATION_STORAGE_MINIO_BUCKET`、`ONLYOFFICE_INTEGRATION_STORAGE_MINIO_ACCESS_KEY`、`ONLYOFFICE_INTEGRATION_STORAGE_MINIO_SECRET_KEY` 这些环境变量入口。
  </action>
  <verify>`cd packages/server && mvn -q -DskipITs -Dtest=OnlyofficeIntegrationPropertiesTest test`</verify>
  <acceptance_criteria>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/config/OnlyofficeIntegrationProperties.java` contains `storage`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/storage/StorageProvider.java` exists</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/storage/StorageProvider.java` contains `LOCAL`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/storage/StorageProvider.java` contains `MINIO`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/resources/application.yml` contains `default-provider`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/resources/application.yml` contains `storage:`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/resources/application.yml` contains `minio:`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/config/OnlyofficeIntegrationPropertiesTest.java` exists</criterion>
  </acceptance_criteria>
  <done>存储 provider、路由配置和环境变量入口已经标准化，后续 provider 切换不再依赖硬编码条件。</done>
</task>

<task id="2-01-02">
  <read_first>
    <file>packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/service/DocumentStorageService.java</file>
    <file>packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/model/RequestContext.java</file>
    <file>.planning/phases/02-storage-strategy-layer/02-CONTEXT.md</file>
    <file>.planning/phases/02-storage-strategy-layer/02-RESEARCH.md</file>
  </read_first>
  <action>
    新增 provider-neutral 的存储合同与 local 实现。具体要求：
    1. 创建 `DocumentStorageStrategy` 接口，接口至少暴露 `provider()`、`exists(String storageKey)`、`read(String storageKey)`、`writeNew(StorageWriteRequest request)`、`overwrite(StorageWriteRequest request)`、`delete(String storageKey)`；
    2. 创建 `StorageWriteRequest` 和 `StoredObjectResource`，字段至少覆盖 `storageKey`、`contentType`、`body`、`size`、`lastModified`；
    3. 新增 `StorageKeyFactory`，固定使用 `tenant/sourceSystem/documentId.ext` 生成对象键，禁止继续返回 `documents/{documentId}.ext` 这种旧结构；
    4. 新增 `LocalDocumentStorageStrategy`，把当前 `resolveStoragePath`、`Files.readAllBytes`、`Files.write`、越权路径校验等 local 逻辑迁移进去。
  </action>
  <verify>`cd packages/server && mvn -q -DskipITs -Dtest=LocalDocumentStorageStrategyTest test`</verify>
  <acceptance_criteria>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/storage/DocumentStorageStrategy.java` exists</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/storage/DocumentStorageStrategy.java` contains `writeNew`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/storage/DocumentStorageStrategy.java` contains `overwrite`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/storage/StorageKeyFactory.java` exists</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/storage/StorageKeyFactory.java` contains `tenant`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/storage/local/LocalDocumentStorageStrategy.java` exists</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/storage/local/LocalDocumentStorageStrategy.java` contains `Files.createDirectories`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/storage/local/LocalDocumentStorageStrategyTest.java` exists</criterion>
  </acceptance_criteria>
  <done>统一存储合同和 local provider 已建立，key 结构切换到 provider-neutral 语义。</done>
</task>

<task id="2-01-03">
  <read_first>
    <file>packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/service/DocumentStorageService.java</file>
    <file>packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/service/DocumentStorageServiceTest.java</file>
    <file>packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/web/DocumentApiControllerTest.java</file>
    <file>.planning/phases/02-storage-strategy-layer/02-VALIDATION.md</file>
  </read_first>
  <action>
    把 `DocumentStorageService` 从直接使用本地文件系统重构为“业务编排层 + strategy 调用层”。具体要求：
    `ensureBootstrapDocument`、`getRequiredDocument`、`readDocument`、`storeUploadedDocument`、`importRemoteDocument`、`createNativeDocument`、`saveCallbackDocument` 全部改为调用 `DocumentStorageStrategy`；
    `DocumentStorageService` 内不再保留 `resolveStoragePath`、`ensureStorageRoot`、`buildStorageKey` 这种本地实现细节；
    读取文档时若元数据存在但 `strategy.exists(storageKey)` 为 false，必须抛出明确错误；
    local provider 仍需完整跑通 bootstrap 文档生成、上传和读取，不允许 Phase 2 第一波就破坏现有开发回退路径。
  </action>
  <verify>`cd packages/server && mvn -q -DskipITs -Dtest=DocumentStorageServiceTest,DocumentApiControllerTest test`</verify>
  <acceptance_criteria>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/service/DocumentStorageService.java` contains `DocumentStorageStrategy`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/service/DocumentStorageService.java` contains `StorageKeyFactory`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/service/DocumentStorageService.java` does not contain `resolveStoragePath`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/service/DocumentStorageService.java` does not contain `buildStorageKey`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/service/DocumentStorageServiceTest.java` contains `tenant-a/native/`</criterion>
    <criterion>`cd packages/server && mvn -q -DskipITs -Dtest=DocumentStorageServiceTest,DocumentApiControllerTest test` exits 0</criterion>
  </acceptance_criteria>
  <done>`DocumentStorageService` 已经只负责编排，底层 local 读写完全通过统一策略接口工作。</done>
</task>

## Verification

- `cd packages/server && mvn -q -DskipITs test`
- `cd packages/server && mvn test`

## Notes for Execution

- 本计划只建立统一合同和 local 适配，不在这里引入 MinIO SDK 调用。
- 若执行中发现 `StoredDocument` 需要补充 storage availability 或 provider 字段，可以新增，但不要把 provider 细节泄漏给 controller 入参。
