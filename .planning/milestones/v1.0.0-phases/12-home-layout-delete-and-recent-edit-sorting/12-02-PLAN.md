---
phase: 12-home-layout-delete-and-recent-edit-sorting
plan: 02
type: homepage-column-ratio-full-height-and-cta-hierarchy-refinement
wave: 1
depends_on: []
files_modified:
  - packages/web/src/pages/DocumentLibraryPage.vue
  - packages/web/src/components/library/DocumentList.vue
  - packages/web/src/components/library/DocumentCreateActions.vue
  - packages/web/src/test/DocumentList.test.js
autonomous: true
requirements:
  - PH12-UI-01
  - LIB-01
  - UI-01
  - QUAL-02
user_setup: []
must_haves:
  - 首页桌面端左右栏比例收口到约 `1/5 : 4/5`
  - 左右主盒子都能随视口高度贴到底边，不再只是自然内容高度
  - `开始编辑` 位于提示语上方，且 `先查看，再决定是否进入编辑` 与右侧说明文字保持相同字号和颜色
---

# Plan 12-02: 首页双栏比例、满高布局与 CTA 层级调整

## Outcome

把工作台首页从“只是左右分栏”进一步收口成稳定的桌面工作台：左右主盒子能贴底、信息层级清晰，文档列表头部的行动入口和说明文案不再互相抢视觉焦点。

## Tasks

<task id="12-02-01">
  <read_first>
    <file>packages/web/src/pages/DocumentLibraryPage.vue</file>
    <file>packages/web/src/components/library/DocumentList.vue</file>
    <file>.planning/quick/260331-kj3-homepage-two-column-layout/260331-kj3-PLAN.md</file>
    <file>.planning/phases/12-home-layout-delete-and-recent-edit-sorting/12-CONTEXT.md</file>
  </read_first>
  <action>
    重构工作台首页的桌面端双栏比例和满高布局。具体要求：
    1. `DocumentLibraryPage.vue` 的桌面端列宽从当前 `10 / 14` 收口为约 `1/5 : 4/5`，推荐直接改成 `5 / 19`；移动端 `xs` 仍然保持 `24 / 24` 单列；
    2. 页面容器、主行和左右列都要切成可伸展布局，让左右主盒子能贴近页面底边；执行时至少要显式处理：
       - `.library-shell`
       - `el-main`
       - 主 `el-row`
       - 左列容器
       - 右列容器
    3. 左侧保持“上下文卡片 + 最近文档卡片”的结构，但最近文档卡片必须吸收剩余高度，而不是继续只按内容高度自然展开；
    4. 右侧保持“筛选工具条 + 列表 + 分页”的结构，列表区域需要能吸收剩余高度，分页条固定贴近底部，不要在桌面端悬空；
    5. 不要破坏 Phase 11 已经落地的分页、创建弹窗和筛选交互。
  </action>
  <verify>`cd packages/web && corepack pnpm test -- --run && corepack pnpm build`</verify>
  <acceptance_criteria>
    <criterion>`packages/web/src/pages/DocumentLibraryPage.vue` contains desktop column sizes equivalent to `5` and `19` or another explicit 1/5:4/5 split</criterion>
    <criterion>`packages/web/src/pages/DocumentLibraryPage.vue` contains flex or height styles that make the left and right column wrappers stretch to the viewport bottom</criterion>
    <criterion>`packages/web/src/pages/DocumentLibraryPage.vue` keeps `DocumentCreateActions` inside the dialog flow rather than restoring the old always-visible card</criterion>
    <criterion>`cd packages/web && corepack pnpm test -- --run` exits 0</criterion>
    <criterion>`cd packages/web && corepack pnpm build` exits 0</criterion>
  </acceptance_criteria>
  <done>首页桌面端已经从“分栏页面”升级成“贴底工作台”，左右主盒子的高度和比例都符合新的布局要求。</done>
</task>

<task id="12-02-02">
  <read_first>
    <file>packages/web/src/components/library/DocumentList.vue</file>
    <file>packages/web/src/components/library/DocumentCreateActions.vue</file>
    <file>packages/web/src/test/DocumentLibraryPage.test.js</file>
    <file>.planning/todos/pending/2026-03-31-homepage-two-column-layout-and-start-edit-entry.md</file>
  </read_first>
  <action>
    收口文档列表头部的 CTA 层级和文案样式，并补一个真正覆盖组件结构的测试。具体要求：
    1. `DocumentList.vue` 里把 `开始编辑` 按钮移动到提示语上方；头部可改为垂直 stack，但最终视觉必须是“按钮在上，说明在下”；
    2. `先查看，再决定是否进入编辑` 不再使用强调型 `<h2>` 样式，而是与右侧说明文案保持一致的字号和颜色，推荐统一复用 `muted-copy` 或等价的次级文案类；
    3. 原先列表头部的主标题层级要重新分配：保留“文档列表”作为 section 标识，但不要再让这句提示语承担页面主标题角色；
    4. 新增 `packages/web/src/test/DocumentList.test.js`（或等价组件级测试），至少覆盖：
       - `开始编辑` 按钮可触发 `start-edit`
       - 提示语渲染为次级文案而不是主要标题
       - 头部结构存在“按钮在上、提示语在下”的 DOM 或 class 证据
  </action>
  <verify>`cd packages/web && corepack pnpm test -- --run`</verify>
  <acceptance_criteria>
    <criterion>`packages/web/src/components/library/DocumentList.vue` no longer renders `先查看，再决定是否进入编辑` as the primary `h2` heading</criterion>
    <criterion>`packages/web/src/components/library/DocumentList.vue` contains `开始编辑` before the hint copy in the header DOM structure</criterion>
    <criterion>`packages/web/src/test/DocumentList.test.js` covers the `start-edit` emit and header copy hierarchy</criterion>
    <criterion>`cd packages/web && corepack pnpm test -- --run` exits 0</criterion>
  </acceptance_criteria>
  <done>列表头部的按钮和说明文案已经回到正确层级，用户会先看到动作入口，再看到“先查看再编辑”的提示。</done>
</task>

## Verification

- `cd packages/web && corepack pnpm test -- --run`
- `cd packages/web && corepack pnpm build`

## Notes for Execution

- 这一计划故意只处理布局和层级，不把“删除接线”混进来；这样 wave 1 可以与后端删除契约并行推进。
- 桌面端需要明显感知到比例变化，但移动端不应因为硬套比例而牺牲可读性。
