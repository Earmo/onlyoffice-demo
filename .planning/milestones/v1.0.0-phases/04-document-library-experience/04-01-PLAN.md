---
phase: 04-document-library-experience
plan: 01
type: fullstack-library-home
wave: 1
depends_on: []
files_modified:
  - packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/web/DocumentApiController.java
  - packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/model/DocumentListResponse.java
  - packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/web/DocumentApiControllerTest.java
  - packages/web/package.json
  - packages/web/src/App.vue
  - packages/web/src/main.js
  - packages/web/src/style.css
  - packages/web/src/router/index.js
  - packages/web/src/pages/DocumentLibraryPage.vue
  - packages/web/src/components/library/DocumentList.vue
autonomous: true
requirements:
  - LIB-01
  - LIB-02
user_setup: []
must_haves:
  - 首页默认显示文档工作台，而不是自动打开固定文档编辑页
  - 列表接口已能承载工作台首页所需的排序、搜索或筛选基础语义
  - 首页具备轻量信息卡、主操作区、最近文档区和主列表的基础结构
---

# Plan 04-01: 建立文档工作台首页与列表查询契约

## Outcome

先把“用户打开首页先看到什么”这件事做扎实：扩展文档列表查询契约，拆出首页工作台壳层、主操作区和主列表，让前端默认进入文档工作台而不是固定编辑器。

## Tasks

<task id="4-01-01">
  <read_first>
    <file>packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/web/DocumentApiController.java</file>
    <file>packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/model/DocumentSummaryResponse.java</file>
    <file>.planning/phases/04-document-library-experience/04-CONTEXT.md</file>
    <file>.planning/phases/04-document-library-experience/04-RESEARCH.md</file>
  </read_first>
  <action>
    扩展现有 `/api/documents` 列表契约，使其足以支撑首页工作台。具体要求：
    1. 列表接口增加工作台首页所需的查询参数，至少覆盖关键词搜索、排序方向，以及围绕现有摘要字段的基础筛选入口；
    2. 搜索与筛选必须建立在现有列表接口上，不新增第二套独立搜索 feed；
    3. 返回结果继续保留 `storageAvailable`、`status`、`actor`、`lastSavedTime` 等 Phase 2/3 已有语义；
    4. `DocumentApiControllerTest` 至少覆盖“默认排序”“带关键词查询”“带筛选参数”三类路径。
  </action>
  <verify>`cd packages/server && mvn -q -DskipITs -Dtest=DocumentApiControllerTest test`</verify>
  <acceptance_criteria>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/web/DocumentApiController.java` contains `@RequestParam`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/web/DocumentApiController.java` contains `query` or `keyword`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/web/DocumentApiControllerTest.java` contains `list`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/web/DocumentApiControllerTest.java` contains `storageAvailable`</criterion>
  </acceptance_criteria>
  <done>工作台首页所需的列表查询契约已经具备，后续前端不需要自己拼第二套搜索/筛选数据源。</done>
</task>

<task id="4-01-02">
  <read_first>
    <file>packages/web/src/App.vue</file>
    <file>packages/web/src/main.js</file>
    <file>packages/web/src/style.css</file>
    <file>.planning/phases/04-document-library-experience/04-CONTEXT.md</file>
  </read_first>
  <action>
    把前端入口从“单页编辑器宿主”拆成“工作台首页壳层”。具体要求：
    1. 引入基础路由或等价页面分层，为 `/` 和后续 `/editor/:id` 预留清晰结构；
    2. 首页必须默认进入文档工作台，而不是自动请求固定文档 editor-config；
    3. 工作台首页至少包含：轻量信息卡、顶部主操作区、最近文档区、主列表区域和列表内容区的错误/空状态骨架；
    4. 视觉上保持当前仓库已有的设计语言，但把“控制台附属动作”升级成真正首页入口。
  </action>
  <verify>`cd packages/web && pnpm build`</verify>
  <acceptance_criteria>
    <criterion>`packages/web/src/main.js` contains `router` or equivalent page split entry</criterion>
    <criterion>`packages/web/src/App.vue` no longer directly hardcodes `demo` as default home behavior</criterion>
    <criterion>`packages/web/src/pages/DocumentLibraryPage.vue` exists</criterion>
    <criterion>`packages/web/src/components/library/DocumentList.vue` exists</criterion>
  </acceptance_criteria>
  <done>首页已经变成文档工作台壳层，后续编辑器页和创建动作都有清晰落点。</done>
</task>

<task id="4-01-03">
  <read_first>
    <file>packages/web/src/pages/DocumentLibraryPage.vue</file>
    <file>packages/web/src/components/library/DocumentList.vue</file>
    <file>packages/web/src/style.css</file>
    <file>.planning/phases/04-document-library-experience/04-CONTEXT.md</file>
  </read_first>
  <action>
    把工作台首页与列表接口真正接通。具体要求：
    1. 首页加载时请求文档列表接口，而不是请求 editor-config；
    2. 最近文档区基于当前列表数据投影，不新增独立活动流接口；
    3. 列表项直接展示标题、类型、状态、最近保存时间和异常标签；
    4. 空状态强调“新建或上传第一个文档”，错误态提供重试按钮，但不把整页打成报错页。
  </action>
  <verify>`cd packages/web && pnpm build`</verify>
  <acceptance_criteria>
    <criterion>`packages/web/src/pages/DocumentLibraryPage.vue` contains `/api/documents`</criterion>
    <criterion>`packages/web/src/pages/DocumentLibraryPage.vue` contains `retry` or `重新加载`</criterion>
    <criterion>`packages/web/src/components/library/DocumentList.vue` contains `storageAvailable`</criterion>
    <criterion>`packages/web/src/components/library/DocumentList.vue` contains `lastSavedTime`</criterion>
  </acceptance_criteria>
  <done>首页已真正成为基于文档列表 API 的工作台，而不是编辑器附属抽屉。</done>
</task>

## Verification

- `cd packages/server && mvn -q -DskipITs -Dtest=DocumentApiControllerTest test`
- `cd packages/web && pnpm build`
- `cd packages/server && mvn test`

## Notes for Execution

- 本计划只建立首页工作台壳层和列表查询基础，不在这里直接完成编辑页切换链路。
- 如果实现中需要新增前端组件或路由目录，优先拆分现有 `App.vue`，不要继续把所有工作台状态堆回单文件组件。
