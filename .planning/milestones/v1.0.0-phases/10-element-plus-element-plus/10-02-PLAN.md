---
wave: 2
depends_on: [10-01-PLAN.md]
files_modified:
  - packages/web/src/pages/DocumentLibraryPage.vue
  - packages/web/src/pages/DocumentEditorPage.vue
  - packages/web/src/pages/DocumentPreviewPage.vue
  - packages/web/src/components/editor/EditorShell.vue
autonomous: true
---

# 10-02. Core Page & Layout Refactor

## Objective
Refactor the main pages and shell container components to use standard Element Plus layout wrappers (`el-container`, `el-header`, `el-main`, `el-aside`).

## Requirements
- `DocumentLibraryPage` acts as the root workbench layout.
- `EditorShell` wrapper inside `DocumentEditorPage` uses strict fixed layouts utilizing `el-container`.

## Tasks

<task>
<action>
Modify `packages/web/src/pages/DocumentLibraryPage.vue`.
Replace the outermost custom layout div with `<el-container>` wrapping `<el-header>` (for the title/"只读信息" card etc.) and `<el-main>` (wrapping the `DocumentCreateActions` and `DocumentList` components).
</action>
<read_first>
- packages/web/src/pages/DocumentLibraryPage.vue
- .planning/phases/10-element-plus-element-plus/10-UI-SPEC.md
</read_first>
<acceptance_criteria>
- `cat packages/web/src/pages/DocumentLibraryPage.vue` contains `<el-container>` and `<el-header>`.
</acceptance_criteria>
</task>

<task>
<action>
Modify `packages/web/src/components/editor/EditorShell.vue` and `packages/web/src/pages/DocumentEditorPage.vue`.
Refactor the shell to use `<el-container>`.
If there's a sidebar (drawers/sidebars for configs), use `<el-aside>`.
Use `<el-page-header>` for the return/back button in the document header.
</action>
<read_first>
- packages/web/src/components/editor/EditorShell.vue
- packages/web/src/pages/DocumentEditorPage.vue
</read_first>
<acceptance_criteria>
- `grep -r "el-page-header" packages/web/src/components/editor` returns a match.
- Custom flex wrapper layouts for sidebar panels are stripped in favor of `<el-aside>`.
</acceptance_criteria>
</task>

## Verification
- Page transitions continue working perfectly under layout `<el-container>`.
- Sidebar components do not break out of their fixed flex boxes (Element `el-container` with `style="height: 100vh"` handles this automatically).
