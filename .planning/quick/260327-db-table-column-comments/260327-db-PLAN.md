# Quick Task 260327-db: 给数据库所有的表和字段都加上 comment 注释

## Tasks

### 1. 盘点当前数据库 migration 覆盖的真实表结构
- 文件：
  - `packages/server/onlyoffice-integration-data/src/main/resources/db/migration/V1__create_document_metadata.sql`
  - `packages/server/onlyoffice-integration-data/src/main/resources/db/migration/V3__create_access_audit_event.sql`
  - `packages/server/onlyoffice-integration-data/src/main/resources/db/migration/V4__create_document_runtime_event.sql`
  - `packages/server/onlyoffice-integration-data/src/main/resources/db/migration/V5__create_document_editor_session.sql`
- 动作：
  - 确认当前仓库需要补注释的表与字段范围，避免漏掉后续阶段新增的数据表
- 验证：
  - 人工核对 migration 中的表结构与字段列表
- 完成标准：
  - 可以明确列出所有需要补充数据库注释的表和字段

### 2. 通过新增 Flyway migration 为所有表和字段补充注释
- 文件：
  - `packages/server/onlyoffice-integration-data/src/main/resources/db/migration/V6__add_table_and_column_comments.sql`
- 动作：
  - 新增一个只负责 `comment on table` 和 `comment on column` 的 migration，为当前所有表和字段补齐中文注释
- 验证：
  - 人工检查 SQL 是否覆盖全部表与字段
- 完成标准：
  - 新库初始化和旧库升级后，所有数据库表和字段都具备清晰 comment

### 3. 验证新 migration 不影响服务端测试链路
- 文件：
  - `packages/server/pom.xml`
- 动作：
  - 运行服务端测试，确认 Flyway 可以顺利执行新增注释 migration
- 验证：
  - `cd packages/server && mvn -q -pl onlyoffice-integration-service -am -DskipITs test`
- 完成标准：
  - 服务端测试全部通过，说明数据库注释 migration 与现有初始化流程兼容
