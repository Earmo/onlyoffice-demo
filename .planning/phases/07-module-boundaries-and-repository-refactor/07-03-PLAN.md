---
phase: 07-module-boundaries-and-repository-refactor
plan: 03
type: naming-and-integration
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - packages/server/onlyoffice-integration-service/src/main/java/**/OnlyofficeIntegrationStarterApplication.java
  - packages/server/onlyoffice-integration-service/src/main/java/**/OnlyofficeIntegrationProperties.java
  - packages/server/onlyoffice-integration-service/src/main/resources/application.yml
  - packages/server/onlyoffice-integration-service/src/test/resources/application.yml
  - packages/server/Dockerfile
  - README.md
  - docs/minimal-integration.md
autonomous: true
requirements:
  - ARCH-04
  - MOD-01
user_setup: []
must_haves:
  - 服务入口、artifact、配置前缀和环境变量不再继续使用 `demo` 命名
  - service 模块成功依赖 data 模块并跑通现有接口与测试
  - `onlyoffice-integration-starter` 成为对外统一名称
---

# Plan 07-03: 清理 demo 命名并接通 starter 服务模块

## Outcome

在多模块和 repository 重构完成后，把对外可见的类名、artifact、配置和文档统一收敛到 `onlyoffice-integration-starter` 语义，避免旧命名继续扩散。

## Tasks

<task id="7-03-01">
  <read_first>
    <file>packages/server/pom.xml</file>
    <file>packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/OnlyofficeIntegrationStarterApplication.java</file>
    <file>packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/config/OnlyofficeIntegrationProperties.java</file>
    <file>.planning/phases/07-module-boundaries-and-repository-refactor/07-RESEARCH.md</file>
  </read_first>
  <action>
    将后端核心命名从 demo 风格改为 starter 风格。
    具体目标状态：
    启动类重命名为 `OnlyofficeIntegrationStarterApplication`；
    `DemoProperties` 重命名为 `OnlyofficeIntegrationProperties`；
    模块与 artifact 名使用 `onlyoffice-integration-data`、`onlyoffice-integration-service`、`onlyoffice-integration-starter`；
    包路径从 `com.earmo.onlyoffice.demo` 收敛到 `com.earmo.onlyoffice.integration`，并同步修复所有 import、扫描路径、测试类和 APT 生成路径来源。
  </action>
  <verify>`cd packages/server && mvn -q -DskipITs test`</verify>
  <acceptance_criteria>
    <criterion>`packages/server/**/OnlyofficeIntegrationStarterApplication.java` exists</criterion>
    <criterion>`packages/server/**/OnlyofficeIntegrationProperties.java` exists</criterion>
    <criterion>`packages/server/pom.xml` contains `onlyoffice-integration-starter`</criterion>
    <criterion>`packages/server` source tree does not contain `OnlyofficeDemoApplication.java`</criterion>
    <criterion>`packages/server` source tree does not contain `DemoProperties.java`</criterion>
  </acceptance_criteria>
  <done>核心入口与构建名称完成 starter 化，新的名称体系从类名到 artifact 保持一致。</done>
</task>

<task id="7-03-02">
  <read_first>
    <file>packages/server/onlyoffice-integration-service/src/main/resources/application.yml</file>
    <file>packages/server/onlyoffice-integration-service/src/test/resources/application.yml</file>
    <file>docker-compose.yml</file>
    <file>README.md</file>
  </read_first>
  <action>
    将配置根和环境变量从 `demo` 收敛到新的 starter 命名，并同步所有运行入口。
    具体要求：
    `application.yml` 与测试配置的根前缀改成 `onlyoffice.integration` 或另一处明确的 starter 语义前缀，不能继续使用裸 `demo:`；
    环境变量从 `DEMO_PUBLIC_BASE_URL`、`DEMO_INTERNAL_BASE_URL`、`DEMO_ONLYOFFICE_*` 收敛到统一的 `ONLYOFFICE_INTEGRATION_*` 命名；
    `docker-compose.yml`、README、`docs/minimal-integration.md` 中的示例配置也同步切换，不允许代码和文档各说各话。
  </action>
  <verify>`cd packages/server && mvn -q -DskipITs -Dtest=*PropertiesTest,DocumentApiControllerTest test`</verify>
  <acceptance_criteria>
    <criterion>`packages/server/**/application.yml` does not contain a root key `demo:`</criterion>
    <criterion>`packages/server/**/application.yml` contains `onlyoffice:` or `integration:` starter-style root keys</criterion>
    <criterion>`docker-compose.yml` contains `ONLYOFFICE_INTEGRATION_`</criterion>
    <criterion>`README.md` does not contain `onlyoffice-demo-server`</criterion>
    <criterion>`docs/minimal-integration.md` contains `onlyoffice-integration-starter`</criterion>
  </acceptance_criteria>
  <done>配置命名、环境变量和接入文档统一切到 starter 语义，避免新旧命名混杂。</done>
</task>

<task id="7-03-03">
  <read_first>
    <file>packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/web/DocumentApiControllerTest.java</file>
    <file>packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/service/DocumentStorageServiceTest.java</file>
    <file>.planning/phases/07-module-boundaries-and-repository-refactor/07-VALIDATION.md</file>
    <file>.planning/ROADMAP.md</file>
  </read_first>
  <action>
    完成 service 模块对 data 模块的最终接线并做命名残留扫描。
    执行时需要：
    确认所有 service、controller、config、test 都通过 data 模块导出的实体、repository 和 mapper 工作；
    更新测试包路径和 import，使 MVC、service、storage 测试在新包名与新模块路径下继续通过；
    运行 `rg -n "onlyoffice-demo|OnlyofficeDemo|demo:|@Select\\(" packages/server README.md docs --glob "!**/target/**"`，把结果收敛到 0 条可执行代码命中，若保留历史说明，必须只出现在规划或历史文档里。
  </action>
  <verify>`cd packages/server && mvn test`</verify>
  <acceptance_criteria>
    <criterion>`cd packages/server && mvn test` exits 0</criterion>
    <criterion>`rg -n "onlyoffice-demo|OnlyofficeDemo|demo:|@Select\\(" packages/server README.md docs --glob "!**/target/**"` returns no hits in executable code files</criterion>
    <criterion>`packages/server/**/DocumentApiControllerTest.java` compiles under the new package path</criterion>
    <criterion>service module source imports at least one class from data module packages</criterion>
  </acceptance_criteria>
  <done>新模块、新命名和数据访问层已经真正接通，starter 形态在构建、运行和测试上都闭环可用。</done>
</task>

## Verification

- `cd packages/server && mvn test`
- `rg -n "onlyoffice-demo|OnlyofficeDemo|demo:|@Select\\(" packages/server README.md docs --glob "!**/target/**"`

## Notes for Execution

- 这一步是统一收口，不要再混入新的存储策略或用户上下文特性。
- 若需要兼容旧环境变量，请显式记录兼容期和默认值，避免无声破坏现有部署。
