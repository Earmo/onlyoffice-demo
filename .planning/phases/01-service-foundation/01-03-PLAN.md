---
phase: 01-service-foundation
plan: 03
type: backend-api
wave: 2
depends_on:
  - 01
  - 02
files_modified:
  - packages/server/src/main/java/com/earmo/onlyoffice/demo/web/DocumentController.java
  - packages/server/src/main/java/com/earmo/onlyoffice/demo/web/DocumentApiController.java
  - packages/server/src/main/java/com/earmo/onlyoffice/demo/web/RequestContextResolver.java
  - packages/server/src/main/java/com/earmo/onlyoffice/demo/model/DocumentSummaryResponse.java
  - packages/server/src/main/java/com/earmo/onlyoffice/demo/model/EditorConfigResponse.java
  - packages/server/src/main/java/com/earmo/onlyoffice/demo/model/CreateDocumentRequest.java
  - packages/server/src/main/java/com/earmo/onlyoffice/demo/model/DocumentListResponse.java
  - packages/server/src/test/java/com/earmo/onlyoffice/demo/web/DocumentApiControllerTest.java
  - packages/server/src/test/java/com/earmo/onlyoffice/demo/web/RequestContextResolverTest.java
  - docs/minimal-integration.md
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
user_setup: []
must_haves:
  - 对外 API 明确区分 list/create/open/editor-config/callback 语义
  - 上游系统可以通过服务端 API 建立文档上下文并获得内部 documentId
  - 标准化用户上下文解析与业务 API 解耦，不把认证实现写死到文档核心
---

# Plan 01-03: 梳理对外接口和微服务接入边界

## Outcome

让文档服务正式暴露“可被集成的 API 边界”，而不是继续把 demo 编辑流程当默认契约。

## Tasks

<task id="1-03-01">
  <read_first>
    <file>packages/server/src/main/java/com/earmo/onlyoffice/demo/web/DocumentController.java</file>
    <file>packages/server/src/main/java/com/earmo/onlyoffice/demo/model/DocumentSummaryResponse.java</file>
    <file>packages/server/src/main/java/com/earmo/onlyoffice/demo/model/EditorConfigResponse.java</file>
    <file>.planning/phases/01-service-foundation/01-CONTEXT.md</file>
    <file>.planning/phases/01-service-foundation/01-RESEARCH.md</file>
  </read_first>
  <action>
    调整现有控制器结构，把“文档主数据 API”和“ONLYOFFICE 运行时回调 / 文件流接口”从语义上拆开。
    可以保留 `DocumentController` 处理 ONLYOFFICE `file`、`callback`、`editor-config`，同时新增 `DocumentApiController` 处理：
    `GET /api/documents`、
    `POST /api/documents`、
    `POST /api/documents/upload`、
    `POST /api/documents/import-remote`、
    `GET /api/documents/{documentId}`。
    `POST /api/documents` 只负责显式创建文档或建立文档上下文，禁止实现“open 时隐式 auto-create”。
    列表和详情返回值中必须包含 `documentId`、`title`、`status`、`sourceSystem`、`ownerUserId`、`tenantId` 或同等语义字段。
  </action>
  <verify>`cd packages/server && mvn -q -DskipITs -Dtest=DocumentApiControllerTest test`</verify>
  <acceptance_criteria>
    <criterion>`packages/server/src/main/java/com/earmo/onlyoffice/demo/web/DocumentApiController.java` exists</criterion>
    <criterion>`packages/server/src/main/java/com/earmo/onlyoffice/demo/web/DocumentApiController.java` contains `@GetMapping`</criterion>
    <criterion>`packages/server/src/main/java/com/earmo/onlyoffice/demo/web/DocumentApiController.java` contains `@PostMapping`</criterion>
    <criterion>`packages/server/src/main/java/com/earmo/onlyoffice/demo/web/DocumentApiController.java` contains `/api/documents`</criterion>
    <criterion>`packages/server/src/main/java/com/earmo/onlyoffice/demo/model/CreateDocumentRequest.java` exists</criterion>
    <criterion>`packages/server/src/main/java/com/earmo/onlyoffice/demo/model/DocumentListResponse.java` exists</criterion>
    <criterion>`packages/server/src/test/java/com/earmo/onlyoffice/demo/web/DocumentApiControllerTest.java` exists</criterion>
  </acceptance_criteria>
  <done>文档主数据 API 与 ONLYOFFICE 运行时接口边界已清晰拆开。</done>
</task>

<task id="1-03-02">
  <read_first>
    <file>packages/server/src/main/java/com/earmo/onlyoffice/demo/web/DocumentController.java</file>
    <file>packages/server/src/main/java/com/earmo/onlyoffice/demo/config/DemoProperties.java</file>
    <file>.planning/phases/01-service-foundation/01-CONTEXT.md</file>
  </read_first>
  <action>
    新建标准化请求上下文解析组件，例如 `RequestContextResolver`，从请求头、服务端 token claim 或显式上下文字段中解析：
    `tenantId`、`sourceSystem`、`externalUserId`、`displayName`。
    v1 头部命名固定为：
    `X-Tenant-Id`、`X-Source-System`、`X-External-User-Id`、`X-User-Display-Name`。
    v1 不做强耦合认证实现，只定义统一解析出口和默认回退规则。
    控制器和服务层调用时，不再直接写死 `demo-user` 作为业务用户来源；若没有外部上下文，则回退到配置中的默认 demo 用户，但回退行为必须集中在 resolver 或上下文对象里，不能散落在多个 service。
  </action>
  <verify>`cd packages/server && mvn -q -DskipITs -Dtest=RequestContextResolverTest test`</verify>
  <acceptance_criteria>
    <criterion>`packages/server/src/main/java/com/earmo/onlyoffice/demo/web/RequestContextResolver.java` exists</criterion>
    <criterion>`packages/server/src/main/java/com/earmo/onlyoffice/demo/web/RequestContextResolver.java` contains `X-Tenant-Id`</criterion>
    <criterion>`packages/server/src/main/java/com/earmo/onlyoffice/demo/web/RequestContextResolver.java` contains `X-Source-System`</criterion>
    <criterion>`packages/server/src/main/java/com/earmo/onlyoffice/demo/web/RequestContextResolver.java` contains `X-External-User-Id`</criterion>
    <criterion>`packages/server/src/main/java/com/earmo/onlyoffice/demo/web/RequestContextResolver.java` contains `X-User-Display-Name`</criterion>
    <criterion>`packages/server/src/test/java/com/earmo/onlyoffice/demo/web/RequestContextResolverTest.java` exists</criterion>
    <criterion>`packages/server/src/main/java/com/earmo/onlyoffice/demo/service/OnlyofficeConfigService.java` contains `defaultUserId`</criterion>
  </acceptance_criteria>
  <done>标准化用户上下文解析已有唯一入口，后续接入外部用户体系不必重写文档核心逻辑。</done>
</task>

<task id="1-03-03">
  <read_first>
    <file>docs/minimal-integration.md</file>
    <file>packages/server/src/main/java/com/earmo/onlyoffice/demo/web/DocumentApiController.java</file>
    <file>packages/server/src/main/java/com/earmo/onlyoffice/demo/web/DocumentController.java</file>
    <file>.planning/phases/01-service-foundation/01-CONTEXT.md</file>
  </read_first>
  <action>
    更新 `docs/minimal-integration.md`，把接入说明从 demo 入口改成“服务端优先”的微服务接入说明。
    文档中至少要明确：
    1. 上游系统先调用 `create` 或 `import` API 建立文档上下文，再拿到内部 `documentId`；
    2. 官方前端只是客户端之一，纯 API 集成也是正式路径；
    3. `editor-config`、`file`、`callback` 的绝对地址由后端生成；
    4. 用户上下文以服务到服务透传为主，默认字段包括 `tenantId`、`sourceSystem`、`externalUserId`；
    5. 打开文档不会隐式 auto-create。
  </action>
  <verify>`cd packages/server && mvn test`</verify>
  <acceptance_criteria>
    <criterion>`docs/minimal-integration.md` contains `documentId`</criterion>
    <criterion>`docs/minimal-integration.md` contains `create`</criterion>
    <criterion>`docs/minimal-integration.md` contains `import`</criterion>
    <criterion>`docs/minimal-integration.md` contains `tenantId`</criterion>
    <criterion>`docs/minimal-integration.md` contains `sourceSystem`</criterion>
    <criterion>`docs/minimal-integration.md` contains `不会隐式 auto-create`</criterion>
  </acceptance_criteria>
  <done>服务接入文档已反映 headless-first 的正式边界，而不是继续描述最小 demo 流程。</done>
</task>

## Verification

- `cd packages/server && mvn -q -DskipITs test`
- `cd packages/server && mvn test`

## Notes for Execution

- 本计划定义 API 边界与上下文抽象，但不要求在这里完成首页列表 UI。
- 如果执行中发现现有 DTO 无法承载文档状态或上下文字段，应新增 DTO，而不是继续复用过窄的 demo 返回对象。
