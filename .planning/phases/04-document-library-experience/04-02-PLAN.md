---
phase: 04-document-library-experience
plan: 02
type: frontend-editor-navigation
wave: 2
depends_on:
  - 01
files_modified:
  - packages/web/src/router/index.js
  - packages/web/src/App.vue
  - packages/web/src/pages/DocumentLibraryPage.vue
  - packages/web/src/pages/DocumentEditorPage.vue
  - packages/web/src/components/editor/EditorShell.vue
  - packages/web/src/style.css
autonomous: true
requirements:
  - LIB-02
user_setup: []
must_haves:
  - 用户可以从工作台列表进入独立编辑页
  - 编辑页具备明确返回列表入口
  - 从一个文档切换到另一个文档时具备显式确认语义
---

# Plan 04-02: 重构编辑入口与列表-编辑页流转

## Outcome

把“工作台首页”和“独立编辑页”真正分开，建立 `/editor/:id` 路由、返回列表入口和切换文档确认语义，让列表进入编辑器成为明确主流程。

## Tasks

<task id="4-02-01">
  <read_first>
    <file>packages/web/src/main.js</file>
    <file>packages/web/src/App.vue</file>
    <file>packages/web/src/pages/DocumentLibraryPage.vue</file>
    <file>.planning/phases/04-document-library-experience/04-CONTEXT.md</file>
  </read_first>
  <action>
    建立独立编辑页路由和页面壳层。具体要求：
    1. 明确支持 `/editor/:id` 路由；
    2. 当前编辑器相关逻辑从首页工作台中拆出，迁移到专门的编辑页或编辑页壳层组件；
    3. 编辑页保留当前项目已有的 ONLYOFFICE 编辑能力、保存状态展示和必要控制入口；
    4. 首页与编辑页的职责边界清晰，不再是一个页面同时承担两套主语义。
  </action>
  <verify>`cd packages/web && pnpm build`</verify>
  <acceptance_criteria>
    <criterion>`packages/web/src/router/index.js` contains `/editor/:id`</criterion>
    <criterion>`packages/web/src/pages/DocumentEditorPage.vue` exists</criterion>
    <criterion>`packages/web/src/components/editor/EditorShell.vue` exists</criterion>
  </acceptance_criteria>
  <done>编辑页已经拥有独立路由和页面边界，首页不再承担默认编辑器宿主职责。</done>
</task>

<task id="4-02-02">
  <read_first>
    <file>packages/web/src/components/library/DocumentList.vue</file>
    <file>packages/web/src/pages/DocumentLibraryPage.vue</file>
    <file>packages/web/src/pages/DocumentEditorPage.vue</file>
    <file>.planning/phases/04-document-library-experience/04-CONTEXT.md</file>
  </read_first>
  <action>
    打通从列表进入编辑页和从编辑页返回列表的主路径。具体要求：
    1. 列表项支持整行进入编辑页；
    2. 危险动作与主点击区域隔离，避免误触；
    3. 编辑页始终有明确“返回文档列表”入口，不能只依赖浏览器回退；
    4. 从列表进入编辑页时，文档标题、状态和必要上下文应延续显示。
  </action>
  <verify>`cd packages/web && pnpm build`</verify>
  <acceptance_criteria>
    <criterion>`packages/web/src/components/library/DocumentList.vue` contains `router.push` or equivalent navigation</criterion>
    <criterion>`packages/web/src/pages/DocumentEditorPage.vue` contains `返回文档列表`</criterion>
    <criterion>`packages/web/src/components/library/DocumentList.vue` contains click handler for row entry</criterion>
  </acceptance_criteria>
  <done>列表进入编辑器与编辑页返回首页的导航链路已经建立。</done>
</task>

<task id="4-02-03">
  <read_first>
    <file>packages/web/src/pages/DocumentEditorPage.vue</file>
    <file>packages/web/src/components/editor/EditorShell.vue</file>
    <file>.planning/phases/04-document-library-experience/04-CONTEXT.md</file>
    <file>.planning/phases/04-document-library-experience/04-VALIDATION.md</file>
  </read_first>
  <action>
    落地编辑页内的文档切换确认语义。具体要求：
    1. 当用户在编辑页尝试切换到另一份文档时，需要显式确认；
    2. 确认逻辑可以基于当前阶段的前端页面状态实现，不强求本阶段接入复杂未保存检测；
    3. 确认文案要清晰说明会离开当前文档；
    4. 该交互要被纳入手动验证清单。
  </action>
  <verify>`cd packages/web && pnpm build`</verify>
  <acceptance_criteria>
    <criterion>`packages/web/src/pages/DocumentEditorPage.vue` contains confirmation logic</criterion>
    <criterion>`packages/web/src/pages/DocumentEditorPage.vue` contains switch document intent</criterion>
    <criterion>`.planning/phases/04-document-library-experience/04-VALIDATION.md` contains `切换文档确认`</criterion>
  </acceptance_criteria>
  <done>编辑页内的文档切换不再是无提示硬切换，用户能明确确认离开当前文档。</done>
</task>

## Verification

- `cd packages/web && pnpm build`
- `cd packages/server && mvn test`

## Notes for Execution

- 本计划聚焦“怎么进入编辑器、怎么返回、怎么切换”，不在这里完成创建动作回流列表。
- 若实现中需要复用当前 `App.vue` 里的编辑器逻辑，优先抽成 `EditorShell` 或同等组件，不要把路由控制和编辑器协议混在一起。
