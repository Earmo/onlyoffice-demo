---
phase: 03-user-context-integration
plan: 03
type: backend-audit-and-extension
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - packages/server/onlyoffice-integration-data/src/main/resources/db/migration/V3__create_access_audit_event.sql
  - packages/server/onlyoffice-integration-data/src/main/java/com/earmo/onlyoffice/integration/data/entity/AccessAuditEventEntity.java
  - packages/server/onlyoffice-integration-data/src/main/java/com/earmo/onlyoffice/integration/data/mapper/AccessAuditEventMapper.java
  - packages/server/onlyoffice-integration-data/src/main/java/com/earmo/onlyoffice/integration/data/repository/AccessAuditEventRepository.java
  - packages/server/onlyoffice-integration-data/src/test/java/com/earmo/onlyoffice/integration/data/repository/AccessAuditRepositoryTest.java
  - packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/service/AccessAuditService.java
  - packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/web/DocumentController.java
  - packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/web/DocumentApiController.java
  - packages/server/onlyoffice-integration-service/src/main/resources/application.yml
  - docs/minimal-integration.md
  - packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/service/AccessAuditServiceTest.java
  - packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/context/CustomAccessContextProviderOverrideTest.java
autonomous: true
requirements:
  - USER-02
  - USER-03
user_setup: []
must_haves:
  - 轻量审计事件已经能记录关键用户路径和 system callback，而不是只剩最终状态
  - starter 已明确如何让外部系统覆盖或扩展访问上下文 provider
  - callback 被如实记录为 system event，不伪装成某个人类用户的直接保存动作
---

# Plan 03-03: 预留外部微服务用户体系对接方式

## Outcome

补齐 Phase 3 的收口层：新增轻量审计事件表和记录服务，把 callback 明确记成 system event，并把外部系统如何覆盖 provider、如何配置解析顺序写进 starter 文档。

## Tasks

<task id="3-03-01">
  <read_first>
    <file>packages/server/onlyoffice-integration-data/src/main/java/com/earmo/onlyoffice/integration/data/entity/DocumentMetadataEntity.java</file>
    <file>packages/server/onlyoffice-integration-data/src/main/java/com/earmo/onlyoffice/integration/data/repository/DocumentMetadataRepository.java</file>
    <file>packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/service/DocumentStatusService.java</file>
    <file>.planning/phases/03-user-context-integration/03-CONTEXT.md</file>
    <file>.planning/phases/03-user-context-integration/03-RESEARCH.md</file>
  </read_first>
  <action>
    在 data/service 模块边界内新增轻量审计事件能力。具体要求：
    1. 新增 Flyway `V3__create_access_audit_event.sql`，表至少包含 `event_id`、`document_id`、`tenant_id`、`source_system`、`actor_user`、`actor_name`、`event_type`、`event_time`、`event_source`、`event_result`、`message`；
    2. 新增 `AccessAuditEventEntity`、`AccessAuditEventMapper`、`AccessAuditEventRepository`；
    3. 新增 `AccessAuditService`，至少提供记录 `document_created`、`document_uploaded`、`document_imported`、`editor_config_requested`、`callback_received` 这些事件的方法；
    4. `callback_received` 事件的 `event_source` 固定写为 `system`，禁止直接写入当前人类用户 ID；
    5. Repository 测试至少覆盖事件写入和按 `documentId` 或 `tenantId` 查询。
  </action>
  <verify>`cd packages/server && mvn -q -DskipITs -Dtest=AccessAuditServiceTest,AccessAuditRepositoryTest test`</verify>
  <acceptance_criteria>
    <criterion>`packages/server/onlyoffice-integration-data/src/main/resources/db/migration/V3__create_access_audit_event.sql` exists</criterion>
    <criterion>`packages/server/onlyoffice-integration-data/src/main/resources/db/migration/V3__create_access_audit_event.sql` contains `actor_user`</criterion>
    <criterion>`packages/server/onlyoffice-integration-data/src/main/resources/db/migration/V3__create_access_audit_event.sql` contains `event_source`</criterion>
    <criterion>`packages/server/onlyoffice-integration-data/src/main/java/com/earmo/onlyoffice/integration/data/entity/AccessAuditEventEntity.java` exists</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/service/AccessAuditService.java` contains `callback_received`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/service/AccessAuditService.java` contains `system`</criterion>
    <criterion>`packages/server/onlyoffice-integration-data/src/test/java/com/earmo/onlyoffice/integration/data/repository/AccessAuditRepositoryTest.java` exists</criterion>
  </acceptance_criteria>
  <done>轻量审计事件表和记录服务已经建立，callback 语义也被诚实地标记为 system event。</done>
</task>

<task id="3-03-02">
  <read_first>
    <file>packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/context/AccessContextResolver.java</file>
    <file>packages/server/onlyoffice-integration-service/src/main/resources/application.yml</file>
    <file>packages/server/onlyoffice-integration-service/pom.xml</file>
    <file>.planning/phases/03-user-context-integration/03-CONTEXT.md</file>
  </read_first>
  <action>
    收口外部微服务用户体系的扩展约定。具体要求：
    1. `AccessContextResolver` 或相应装配类必须支持注入自定义 `AccessContextProvider` Bean，并按 `enabled-providers` / `resolution-order` 配置参与解析；
    2. 当外部系统提供自定义 provider 名称时，必须能覆盖内置 `header`、`jwt`、`default` 顺序，而不是被代码固定忽略；
    3. 新增测试 `CustomAccessContextProviderOverrideTest`，验证自定义 provider 能被注册、参与解析并优先命中；
    4. `application.yml` 或配置类注释中必须明确说明：后续接外部用户中心时，只需要新增 provider 实现和配置，不需要改 controller 或文档业务调用链。
  </action>
  <verify>`cd packages/server && mvn -q -DskipITs -Dtest=CustomAccessContextProviderOverrideTest test`</verify>
  <acceptance_criteria>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/context/AccessContextResolver.java` contains `enabledProviders`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/resources/application.yml` contains `enabled-providers`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/context/CustomAccessContextProviderOverrideTest.java` exists</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/context/CustomAccessContextProviderOverrideTest.java` contains `custom-provider`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/context/AccessContextProvider.java` contains `name`</criterion>
    <criterion>`cd packages/server && mvn -q -DskipITs -Dtest=CustomAccessContextProviderOverrideTest test` exits 0</criterion>
  </acceptance_criteria>
  <done>starter 已经明确支持外部系统通过自定义 provider 对接自己的用户体系。</done>
</task>

<task id="3-03-03">
  <read_first>
    <file>docs/minimal-integration.md</file>
    <file>packages/server/onlyoffice-integration-service/src/main/resources/application.yml</file>
    <file>packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/web/DocumentController.java</file>
    <file>.planning/phases/03-user-context-integration/03-CONTEXT.md</file>
  </read_first>
  <action>
    更新最小接入文档和配置说明，把 Phase 3 的用户接入规则一次性写清。文档中至少要明确：
    1. 官方内置 `header`、`jwt` 两种 provider，且支持注册自定义 provider；
    2. provider 顺序由配置决定，默认值为 `header,jwt,default`；
    3. 完全缺失用户上下文时返回 4xx，部分缺失字段可按 `profile + allow-default-context` 规则补齐；
    4. 最小 permissions map 只覆盖 `edit/comment/download/print`；
    5. callback 审计记录为 `system event`；
    6. 后续接外部用户中心只需要新增 provider 和配置，不需要修改 controller 或文档业务 API。
    同时确保 `application.yml` 注释和接入文档用词一致，避免文档说“严格报错”，代码默认却仍无条件 fallback。
  </action>
  <verify>`cd packages/server && mvn test`</verify>
  <acceptance_criteria>
    <criterion>`docs/minimal-integration.md` contains `header`</criterion>
    <criterion>`docs/minimal-integration.md` contains `jwt`</criterion>
    <criterion>`docs/minimal-integration.md` contains `allow-default-context`</criterion>
    <criterion>`docs/minimal-integration.md` contains `edit/comment/download/print`</criterion>
    <criterion>`docs/minimal-integration.md` contains `system event`</criterion>
    <criterion>`docs/minimal-integration.md` contains `自定义 provider`</criterion>
  </acceptance_criteria>
  <done>外部系统如何接入、如何覆盖 provider、callback 为什么记成 system event 已经在配置与文档中收口一致。</done>
</task>

## Verification

- `cd packages/server && mvn -q -DskipITs test`
- `cd packages/server && mvn test`

## Notes for Execution

- 本计划补的是“轻量审计 + 外部扩展约定”，不是完整审计中心或权限平台。
- 如果执行中发现需要补更多 callback 归因信息，只能记录 system event 与上下文快照，不能直接冒充人类操作者。
