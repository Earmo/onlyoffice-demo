---
phase: "15"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/editor/onlyofficeBridge.js
  - packages/web/dist/onlyoffice-plugins/ai-bridge/code.js
  - packages/web/src/test/onlyofficeBridge.test.js
autonomous: true
requirements:
  - WRIT-01

must_haves:
  truths:
    - "调用 bridge.insertHtml(html) 后，bridge 向插件发送 onlyoffice-ai-bridge:insert-html 类型的消息"
    - "插件收到 insert-html 消息后调用 PasteHtml 并回传 htmlInserted 响应"
    - "bridge 的 pendingRequests 机制能正确 resolve/reject insertHtml 调用"
    - "PasteHtml void callback 被文档注释说明为 best-effort 成功（已知 API 限制）"
    - "code.js 顶部有 MANUALLY MAINTAINED 注释"
    - "insertHtml 超时场景有专项测试覆盖"
  artifacts:
    - path: "packages/web/src/components/editor/onlyofficeBridge.js"
      provides: "insertHtml 事件常量 + bridge 方法"
      contains: "insertHtml"
    - path: "packages/web/dist/onlyoffice-plugins/ai-bridge/code.js"
      provides: "PasteHtml 插件实现"
      contains: "PasteHtml"
    - path: "packages/web/src/test/onlyofficeBridge.test.js"
      provides: "insertHtml 事件单元测试（含 timeout 场景）"
  key_links:
    - from: "onlyofficeBridge.js#insertHtml()"
      to: "code.js#case EVENTS.insertHtml"
      via: "postMessage requestId"
      pattern: "insert-html"
---

<objective>
扩展 ONLYOFFICE 桥接层，为宿主页 → 插件增加 `insertHtml` 命令。宿主页调用 `bridge.insertHtml(html)` 后，插件调用 `PasteHtml` 将 HTML 写入文档当前光标/选区位置，然后回传响应。

Purpose: 为 Phase 15 写回 UI（Plan 02/03）提供稳定的 bridge 接口契约。
Output: 扩展后的 onlyofficeBridge.js、code.js，以及对应测试（含 timeout 场景）。
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-ai-writeback/15-CONTEXT.md

<interfaces>
<!-- 现有 onlyofficeBridge.js 公共契约摘录 -->
```javascript
// onlyofficeBridge.js
export const ONLYOFFICE_AI_BRIDGE_EVENTS = {
  ready: "onlyoffice-ai-bridge:ready",
  error: "onlyoffice-ai-bridge:error",
  captureSelection: "onlyoffice-ai-bridge:capture-selection",
  selectionCaptured: "onlyoffice-ai-bridge:selection-captured",
  refreshOutline: "onlyoffice-ai-bridge:refresh-outline",
  outlineRefreshed: "onlyoffice-ai-bridge:outline-refreshed",
  jumpToHeading: "onlyoffice-ai-bridge:jump-to-heading",
  headingJumped: "onlyoffice-ai-bridge:heading-jumped"
  // Phase 15: 新增 insertHtml / htmlInserted
};

export function createOnlyofficeBridge({ getEditor, getIframe, requestTimeoutMs = 10000 })
  // 返回对象包含: { waitForReady, captureSelectedText, refreshOutline, jumpToHeading, dispose, capability }
  // Phase 15: 增加 insertHtml(html: string): Promise<void>

// 内部 sendRequest 签名（不导出）:
// sendRequest(type: string, payload: object, label: string): Promise<any>
// - 创建 requestId，postMessage 到插件，注册 pendingRequests，超时自动 reject
```

<!-- 现有 code.js 插件端处理模式 -->
```javascript
// code.js (ONLYOFFICE plugin)
// 已有：captureSelection → readSelectedText(requestId)
// 已有：refreshOutline → refreshOutline(requestId)
// 已有：jumpToHeading → jumpToHeading(requestId, payload)
// Phase 15 新增：insertHtml → insertHtmlAtCursor(requestId, payload)

// ONLYOFFICE 插件 API：
// window.Asc.plugin.executeMethod("PasteHtml", [htmlString], callback)
// - 有选区时：替换选区内容
// - 无选区时：在光标位置插入
// callback 无参数（PasteHtml 是 void 方法）
// ⚠ 已知限制：callback 不携带错误参数，无法区分成功/失败；
//   同步异常通过 try/catch 捕获并回传 error 事件；
//   异步失败只能通过 requestTimeoutMs 超时检测。
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 onlyofficeBridge.js 和 code.js 中新增 insertHtml 事件对</name>
  <files>
    packages/web/src/components/editor/onlyofficeBridge.js,
    packages/web/dist/onlyoffice-plugins/ai-bridge/code.js
  </files>
  <read_first>
    - packages/web/src/components/editor/onlyofficeBridge.js
    - packages/web/dist/onlyoffice-plugins/ai-bridge/code.js
  </read_first>
  <behavior>
    - bridge.insertHtml("&lt;p&gt;hello&lt;/p&gt;") 发出 type=insertHtml 的 postMessage
    - bridge pendingRequests 收到 htmlInserted 响应后 resolve
    - bridge 收到 error 响应时 reject，错误信息透传
    - bridge dispose 后调用 insertHtml 立即 reject
    - code.js 顶部有 MANUALLY MAINTAINED 注释
    - PasteHtml void callback 有代码注释说明已知限制
    - insertHtmlAtCursor 用 try/catch 包裹 executeMethod，同步异常回传 error 事件
  </behavior>
  <action>
    **onlyofficeBridge.js 修改（3 处）：**

    1. `ONLYOFFICE_AI_BRIDGE_EVENTS` 对象末尾追加两个事件常量（保持命名风格一致）：
    ```javascript
    insertHtml: "onlyoffice-ai-bridge:insert-html",   // 宿主页请求向文档写入 HTML
    htmlInserted: "onlyoffice-ai-bridge:html-inserted" // 写入结果回传
    ```

    2. `createOnlyofficeBridge()` 返回对象末尾增加 `insertHtml` 方法（与 jumpToHeading 并排）：
    ```javascript
    async insertHtml(html) {
      return sendRequest(
        ONLYOFFICE_AI_BRIDGE_EVENTS.insertHtml,
        { html: typeof html === "string" ? html : "" },
        "写入 HTML 到文档"
      );
    },
    ```
    `sendRequest` 内部会等待 `htmlInserted` 或 `error` 事件响应（通过 requestId 匹配），无需额外代码。

    **code.js 修改（4 处）：**

    1. 文件**第一行**（`var EVENTS = ...` 之前）追加维护声明注释：
    ```javascript
    // MANUALLY MAINTAINED — 此文件不由构建系统生成，须与 onlyofficeBridge.js ONLYOFFICE_AI_BRIDGE_EVENTS 手动保持同步
    ```

    2. `EVENTS` 常量对象末尾追加（与宿主页完全镜像）：
    ```javascript
    insertHtml: "onlyoffice-ai-bridge:insert-html",
    htmlInserted: "onlyoffice-ai-bridge:html-inserted"
    ```

    3. 在 `handleHostMessage` 的 switch 中，`case EVENTS.jumpToHeading:` 之后追加：
    ```javascript
    case EVENTS.insertHtml:
      insertHtmlAtCursor(message.requestId, message.payload || {});
      break;
    ```

    4. 在 `refreshOutline` 函数之后、`handleHostMessage` 之前，新增函数 `insertHtmlAtCursor`：
    ```javascript
    /**
     * 将 HTML 字符串粘贴到文档当前光标位置。
     * ONLYOFFICE PasteHtml 语义：有选区时替换选区，无选区时在光标处插入。
     *
     * ⚠ 已知 API 限制（ONLYOFFICE PasteHtml void callback）：
     *   PasteHtml callback 不携带任何参数，无法区分成功/失败。
     *   此处将 callback 触发视为 best-effort 成功（false-positive 风险低于永久挂起）。
     *   同步异常（如插件未初始化）通过 try/catch 捕获并回传 error 事件。
     *   异步失败（如 ONLYOFFICE 内部错误）只能由宿主页的 requestTimeoutMs 超时检测。
     */
    function insertHtmlAtCursor(requestId, payload) {
      const html = (payload && typeof payload.html === "string") ? payload.html : "";
      try {
        window.Asc.plugin.executeMethod(
          "PasteHtml",
          [html],
          function () {
            // PasteHtml 是 void — callback 无错误参数，视为 best-effort 成功
            postMessage(EVENTS.htmlInserted, { success: true }, requestId);
          }
        );
      } catch (err) {
        // executeMethod 同步抛出时（例如插件未初始化），回传 error 事件
        postMessage(EVENTS.error, { message: err && err.message ? err.message : "insertHtml failed" }, requestId);
      }
    }
    ```
  </action>
  <verify>
    <automated>corepack pnpm --dir packages/web test -- src/test/onlyofficeBridge.test.js --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `ONLYOFFICE_AI_BRIDGE_EVENTS.insertHtml === "onlyoffice-ai-bridge:insert-html"` ✓
    - `ONLYOFFICE_AI_BRIDGE_EVENTS.htmlInserted === "onlyoffice-ai-bridge:html-inserted"` ✓
    - `bridge.insertHtml(html)` 方法存在且返回 Promise ✓
    - `code.js` 第一行包含 `MANUALLY MAINTAINED` 注释 ✓
    - `code.js` 中 `handleHostMessage` 处理 `insertHtml` 消息并调用 `PasteHtml` ✓
    - `insertHtmlAtCursor` 有 try/catch，同步异常回传 error 事件 ✓
    - PasteHtml callback 有注释说明 void / best-effort 限制 ✓
  </acceptance_criteria>
  <done>
    - ONLYOFFICE_AI_BRIDGE_EVENTS 包含 insertHtml 和 htmlInserted 两个常量
    - bridge.insertHtml(html) 方法存在且签名正确
    - code.js 顶部有 MANUALLY MAINTAINED 注释
    - code.js 中 handleHostMessage 处理 insertHtml 消息并调用 PasteHtml
    - insertHtmlAtCursor 用 try/catch 包裹，同步异常回传 error
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展 onlyofficeBridge.test.js，覆盖 insertHtml 请求-响应链路（含 timeout）</name>
  <files>packages/web/src/test/onlyofficeBridge.test.js</files>
  <read_first>
    - packages/web/src/test/onlyofficeBridge.test.js
  </read_first>
  <behavior>
    - 宿主页调用 bridge.insertHtml("&lt;p&gt;test&lt;/p&gt;") 后，pluginWindow 收到 type=insertHtml 的消息
    - 插件回传 htmlInserted 事件（含相同 requestId）后，Promise resolve
    - 插件回传 error 事件后，Promise reject 且 message 透传
    - 插件无响应时，在 requestTimeoutMs 内 Promise reject（timeout 路径）
  </behavior>
  <action>
    参照现有 `captureSelectedText` 测试的结构，在 `describe("onlyofficeBridge")` 内追加三个测试：

    ```javascript
    it("应将 insertHtml 请求发送至插件窗口并在 htmlInserted 响应后 resolve", async () => {
      const pluginWindow = { postMessage: vi.fn() };

      const bridge = createOnlyofficeBridge({
        getEditor: () => null,
        getIframe: () => ({ contentWindow: { postMessage: vi.fn() } }),
        requestTimeoutMs: 1000
      });

      // 模拟 ready 握手
      window.dispatchEvent(new MessageEvent("message", {
        data: { channel: "onlyoffice-ai-bridge", type: ONLYOFFICE_AI_BRIDGE_EVENTS.ready, capability: "plugin" },
        source: pluginWindow
      }));
      await bridge.waitForReady();

      // 发起 insertHtml 请求
      const insertPromise = bridge.insertHtml("<p>Hello World</p>");
      await Promise.resolve();

      // 验证发出了正确消息
      expect(pluginWindow.postMessage).toHaveBeenCalledWith(
        expect.objectContaining({
          channel: "onlyoffice-ai-bridge",
          type: ONLYOFFICE_AI_BRIDGE_EVENTS.insertHtml,
          payload: { html: "<p>Hello World</p>" }
        }),
        "*"
      );

      // 模拟插件回传 htmlInserted
      const sentMessage = pluginWindow.postMessage.mock.calls.find(
        c => c[0].type === ONLYOFFICE_AI_BRIDGE_EVENTS.insertHtml
      )[0];
      window.dispatchEvent(new MessageEvent("message", {
        data: {
          channel: "onlyoffice-ai-bridge",
          type: ONLYOFFICE_AI_BRIDGE_EVENTS.htmlInserted,
          requestId: sentMessage.requestId,
          payload: { success: true }
        },
        source: pluginWindow
      }));

      await expect(insertPromise).resolves.toEqual({ success: true });
      bridge.dispose();
    });

    it("insertHtml 在插件返回 error 时应 reject 并透传错误信息", async () => {
      const pluginWindow = { postMessage: vi.fn() };

      const bridge = createOnlyofficeBridge({
        getEditor: () => null,
        getIframe: () => ({ contentWindow: { postMessage: vi.fn() } }),
        requestTimeoutMs: 1000
      });

      window.dispatchEvent(new MessageEvent("message", {
        data: { channel: "onlyoffice-ai-bridge", type: ONLYOFFICE_AI_BRIDGE_EVENTS.ready, capability: "plugin" },
        source: pluginWindow
      }));
      await bridge.waitForReady();

      const insertPromise = bridge.insertHtml("<p>fail</p>");
      await Promise.resolve();

      const sentMessage = pluginWindow.postMessage.mock.calls.find(
        c => c[0].type === ONLYOFFICE_AI_BRIDGE_EVENTS.insertHtml
      )[0];
      window.dispatchEvent(new MessageEvent("message", {
        data: {
          channel: "onlyoffice-ai-bridge",
          type: ONLYOFFICE_AI_BRIDGE_EVENTS.error,
          requestId: sentMessage.requestId,
          message: "PasteHtml 执行失败"
        },
        source: pluginWindow
      }));

      await expect(insertPromise).rejects.toThrow("PasteHtml 执行失败");
      bridge.dispose();
    });

    it("insertHtml 在插件无响应时应在超时后 reject（requestTimeoutMs）", async () => {
      const pluginWindow = { postMessage: vi.fn() };

      const bridge = createOnlyofficeBridge({
        getEditor: () => null,
        getIframe: () => ({ contentWindow: { postMessage: vi.fn() } }),
        requestTimeoutMs: 50  // 测试用短超时
      });

      window.dispatchEvent(new MessageEvent("message", {
        data: { channel: "onlyoffice-ai-bridge", type: ONLYOFFICE_AI_BRIDGE_EVENTS.ready, capability: "plugin" },
        source: pluginWindow
      }));
      await bridge.waitForReady();

      const insertPromise = bridge.insertHtml("<p>timeout</p>");
      // 不回传任何响应 → 等待超时

      await expect(insertPromise).rejects.toThrow(/timeout|超时/i);
      bridge.dispose();
    });
    ```
  </action>
  <verify>
    <automated>corepack pnpm --dir packages/web test -- src/test/onlyofficeBridge.test.js --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - insertHtml resolve 场景测试 PASS ✓
    - insertHtml reject/error 场景测试 PASS ✓
    - insertHtml timeout 场景测试 PASS（新增，评审 Issue LOW 修复）✓
    - 原有全部 onlyofficeBridge 测试不回退 ✓
  </acceptance_criteria>
  <done>
    所有 onlyofficeBridge 测试通过，包括：
    - insertHtml resolve 测试
    - insertHtml reject/error 测试
    - insertHtml timeout 测试（新增）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 宿主页 → 插件 postMessage | html payload 来自 AI 模型输出，已通过 markdown-it + DOMPurify 处理（Plan 02 负责）；仅向已知 pluginWindow 发送 |
| 插件 → 文档 PasteHtml | ONLYOFFICE 插件沙箱内执行，html 内容由 ONLYOFFICE 自身处理 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Tampering | code.js#insertHtmlAtCursor | accept | 插件运行在 ONLYOFFICE 沙箱内，html 来源已由宿主页通过 markdown-it + DOMPurify 处理；PasteHtml 是 ONLYOFFICE 官方 API，不执行脚本 |
| T-15-01-02 | Spoofing | handleMessage#source check | mitigate | bridge 已有 `pluginWindow` 来源校验（ready 事件设定），后续消息通过 channel + requestId 双重过滤 |
| T-15-01-03 | Denial of Service | sendRequest timeout | mitigate | requestTimeoutMs（默认 10000ms）超时后自动 reject，不会永久挂起 |
| T-15-01-04 | Tampering | code.js 手动维护 | mitigate | 文件顶部 MANUALLY MAINTAINED 注释提醒开发者同步更新 |
</threat_model>

<verification>
```bash
corepack pnpm --dir packages/web test -- src/test/onlyofficeBridge.test.js --reporter=verbose
```
所有 insertHtml 测试通过（含 timeout），原有测试不回退。
</verification>

<success_criteria>
- `ONLYOFFICE_AI_BRIDGE_EVENTS.insertHtml === "onlyoffice-ai-bridge:insert-html"` ✓
- `ONLYOFFICE_AI_BRIDGE_EVENTS.htmlInserted === "onlyoffice-ai-bridge:html-inserted"` ✓
- `bridge.insertHtml("<p>test</p>")` 返回 Promise ✓
- `code.js` 第一行有 `MANUALLY MAINTAINED` 注释 ✓
- `code.js` 中 `PasteHtml` 被调用，`htmlInserted` 被回传 ✓
- `insertHtmlAtCursor` try/catch 同步异常回传 error 事件 ✓
- PasteHtml void callback 代码注释说明 best-effort 限制 ✓
- 全部 onlyofficeBridge 单元测试通过（含 timeout 场景）✓
</success_criteria>

<output>
完成后在 `.planning/phases/15-ai-writeback/` 创建 `15-01-SUMMARY.md`，记录：
- 新增的两个 EVENTS 常量值
- `insertHtml()` 方法签名
- code.js 中 PasteHtml 调用方式及已知限制说明
- 测试执行结果（含 timeout 测试）
</output>
</output>
