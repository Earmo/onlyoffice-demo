---
phase: "15"
plan: "02"
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified:
  - packages/web/src/components/editor/EditorAiWorkbench.vue
  - packages/web/src/stores/writeBackStore.js
  - packages/web/src/test/EditorAiWorkbench.test.js
  - packages/web/package.json
autonomous: true
requirements:
  - WRIT-01
  - WRIT-02

must_haves:
  truths:
    - "每条 status=completed 的 AI 回复下方出现「写入文档」按钮"
    - "点击「写入文档」弹出确认对话框，展示 DOMPurify 净化后的 markdown-it 渲染预览（使用明确的 ALLOWED_TAGS/ALLOWED_ATTR 白名单）"
    - "openWriteBackDialog 冻结 writeBackHasSelection 快照，控制「替换选区」禁用状态（TOCTOU 修复）"
    - "writeBackMode === 'replace' 且 writeBackHasSelection=true 时显示 ElAlert info 警告（选区可能失效提示）"
    - "confirmWriteBack 设置 writeBackStore.status = 'loading'，emit insert-html 携带 html（无回调）"
    - "watch writeBackStore.status：success → 关闭对话框 + ElMessage.success；error → 保持对话框 + ElMessage.error"
    - "写入完成后抽屉保持开启，用户可继续对话"
  artifacts:
    - path: "packages/web/src/components/editor/EditorAiWorkbench.vue"
      provides: "写入文档按钮、确认对话框（Pinia store 反馈模式）、insert-html emit"
      contains: "insert-html"
    - path: "packages/web/src/stores/writeBackStore.js"
      provides: "writeBackStore Pinia store（status/errorMsg 状态）"
      contains: "writeBackStore"
    - path: "packages/web/src/test/EditorAiWorkbench.test.js"
      provides: "写回 UI 单元测试（store 反馈模式）"
  key_links:
    - from: "EditorAiWorkbench.vue#confirmWriteBack()"
      to: "EditorShell.vue#handleInsertHtml()"
      via: "emit('insert-html', { html })"
      pattern: "insert-html"
    - from: "EditorShell.vue#handleInsertHtml()"
      to: "writeBackStore.status"
      via: "writeBackStore.status = 'success'|'error'"
      pattern: "writeBackStore"
    - from: "EditorAiWorkbench.vue#watch(writeBackStore.status)"
      to: "ElMessage / dialog close"
      via: "watch"
      pattern: "writeBackStore"
    - from: "EditorAiWorkbench.vue#openWriteBackDialog()"
      to: "DOMPurify.sanitize(md.render(entry.assistantText))"
      via: "dompurify ALLOWED_TAGS"
      pattern: "DOMPurify.sanitize"
---

<objective>
在 `EditorAiWorkbench.vue` 中实现"写入文档"写回 UI：
1. 每条已完成 AI 回复行动区新增「写入文档」按钮
2. 点击后弹出写入确认对话框（DOMPurify 净化预览 + 插入模式选择 + 选区失效警示）
3. 使用 **Pinia writeBackStore** 管理反馈状态（method B）：confirmWriteBack 设 loading，emit insert-html 无回调；watch store.status 控制 UI 反馈
4. openWriteBackDialog 冻结 hasEmptySelection 快照（TOCTOU 修复，解决评审 Issue #2）
5. 工具栏顶部新增保存状态展示（从 `runtimeContext.saveStatus` 读取）

Purpose: 完成 WRIT-01/02 的前端 UI 半边；EditorShell 负责实际 bridge 调用 + 更新 store（Plan 03）。
评审修复：Issue #1（Pinia store 反馈）+ Issue #2（TOCTOU 冻结快照 + ElAlert 警示）+ Issue #3（DOMPurify ALLOWED_TAGS 白名单）。
Output: 写回 UI 完整可测试，与 Plan 03（EditorShell）并行开发。
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-ai-writeback/15-CONTEXT.md
@.planning/phases/15-ai-writeback/15-01-SUMMARY.md

<interfaces>
```javascript
// EditorAiWorkbench.vue 现有关键接口摘录
defineProps({
  runtimeContext: Object,  // { hasEmptySelection, saveStatus, ... }
})
const emit = defineEmits(["capture-selection", "refresh-outline", "jump-to-heading", "insert-image"])
// Phase 15 追加: "insert-html"

// emit 契约（新增，Pinia store 模式）:
// emit("insert-html", {
//   html: string,   // DOMPurify 净化后的 HTML（明确 ALLOWED_TAGS/ALLOWED_ATTR）
// })
// 结果通过 writeBackStore.status 异步通知（由 EditorShell 更新）

// writeBackStore 接口（Plan 02 新建）：
// { status: 'idle'|'loading'|'success'|'error', errorMsg: '' }

const md = markdownit({ html: true, linkify: true, typographer: true, highlight: ... })
// conversationEntries[i].status: "streaming" | "completed" | "failed"
// conversationEntries[i].assistantText: string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: 创建 writeBackStore Pinia store</name>
  <files>
    packages/web/src/stores/writeBackStore.js
  </files>
  <read_first>
    - packages/web/src/stores/（查看现有 store 文件命名/结构惯例）
    - packages/web/src/main.js（确认 Pinia 已安装）
  </read_first>
  <behavior>
    - writeBackStore 提供 status（'idle'|'loading'|'success'|'error'）和 errorMsg（string）状态
    - 提供 reset() action 将 status 重置为 'idle'、errorMsg 清空
  </behavior>
  <action>
    新建文件 `packages/web/src/stores/writeBackStore.js`，内容如下：

    ```javascript
    import { defineStore } from "pinia";

    /**
     * 写回文档操作的反馈状态 store。
     * EditorAiWorkbench 监听 status 变化以控制 loading/success/error UI；
     * EditorShell.handleInsertHtml 负责更新此 store。
     *
     * 状态流转：
     *   idle → loading（confirmWriteBack 触发）
     *   loading → success（bridge.insertHtml 成功）
     *   loading → error（bridge 未就绪 或 insertHtml 抛出异常）
     *   success|error → idle（watch 处理完毕后 reset）
     */
    export const useWriteBackStore = defineStore("writeBack", {
      state: () => ({
        /** @type {'idle'|'loading'|'success'|'error'} */
        status: "idle",
        /** 仅 status==='error' 时有值 */
        errorMsg: "",
      }),
      actions: {
        reset() {
          this.status = "idle";
          this.errorMsg = "";
        },
      },
    });
    ```
  </action>
  <verify>
    <automated>corepack pnpm --dir packages/web test -- src/test/EditorAiWorkbench.test.js --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - 文件 `packages/web/src/stores/writeBackStore.js` 存在 ✓
    - 导出 `useWriteBackStore`，state 包含 `status` 和 `errorMsg` ✓
    - `reset()` action 将 status 重置为 'idle' ✓
  </acceptance_criteria>
  <done>
    writeBackStore.js 文件创建，结构符合 Pinia defineStore 惯例
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: 安装 DOMPurify + 写入文档按钮 + 确认对话框（Pinia store 反馈模式）</name>
  <files>
    packages/web/src/components/editor/EditorAiWorkbench.vue,
    packages/web/package.json
  </files>
  <read_first>
    - packages/web/src/components/editor/EditorAiWorkbench.vue
    - packages/web/package.json
    - packages/web/src/stores/writeBackStore.js
  </read_first>
  <behavior>
    - status === 'completed' 的条目显示「写入文档」按钮
    - 点击后弹出确认对话框：DOMPurify 净化后 HTML 预览 + 模式选择
    - openWriteBackDialog 冻结 writeBackHasSelection 快照（TOCTOU 修复，评审 Issue #2）
    - 「替换当前选区」使用 writeBackHasSelection（冻结值）控制禁用状态，而非实时 runtimeContext.hasEmptySelection
    - writeBackMode === 'replace' 且 writeBackHasSelection=true 时显示 ElAlert info 警示
    - confirmWriteBack: writeBackStore.status='loading' → emit('insert-html', { html }) → 保持对话框开启
    - watch writeBackStore.status：'success' → 关闭对话框 + ElMessage.success + store.reset()
    - watch writeBackStore.status：'error' → 保持对话框开启 + ElMessage.error(store.errorMsg) + store.reset()
  </behavior>
  <action>
    **步骤 0：安装 DOMPurify（如 package.json 中尚未存在）**
    ```bash
    corepack pnpm --dir packages/web add dompurify
    corepack pnpm --dir packages/web add -D @types/dompurify
    ```

    **步骤 1：在 script setup 顶部 import 区追加**
    ```javascript
    import DOMPurify from "dompurify";
    import { useWriteBackStore } from "@/stores/writeBackStore";
    ```

    **步骤 2：DOMPurify 白名单常量（在 import 之后、setup 逻辑之前声明）**
    ```javascript
    // DOMPurify 白名单：仅允许基础文档标记，禁止 script/iframe/事件属性等（评审 Issue #3）
    const WRITE_BACK_ALLOWED_TAGS = [
      "p", "strong", "em", "ul", "ol", "li",
      "h1", "h2", "h3", "h4", "h5", "h6",
      "code", "pre", "blockquote", "a", "br",
      "table", "thead", "tbody", "tr", "th", "td"
    ];
    const WRITE_BACK_ALLOWED_ATTR = ["href"];
    ```

    **步骤 3：在 script setup 内初始化 store + ref 状态**
    ```javascript
    const writeBackStore = useWriteBackStore();

    // 写回确认对话框状态
    const writeBackDialogVisible = ref(false);
    const writeBackHtml = ref("");           // DOMPurify.sanitize(md.render(...))
    const writeBackMode = ref("cursor");     // "cursor" | "replace"
    // TOCTOU 修复（评审 Issue #2）：在对话框打开时冻结选区快照，
    // 避免 ElDialog 获取焦点后 runtimeContext.hasEmptySelection 变为 true 导致禁用状态误判
    const writeBackHasSelection = ref(false);
    ```

    **步骤 4：defineEmits 追加 insert-html**
    ```javascript
    const emit = defineEmits([
      "capture-selection", "refresh-outline", "jump-to-heading", "insert-image",
      "insert-html"
    ]);
    ```

    **步骤 5：新增 openWriteBackDialog(entry) 函数**
    ```javascript
    function openWriteBackDialog(entry) {
      // 冻结快照：在对话框打开前记录当前选区状态（TOCTOU 修复，评审 Issue #2）
      // 对话框打开后焦点转移到 Vue 宿主页，ONLYOFFICE 选区会失效，
      // 此时 runtimeContext.hasEmptySelection 将变为 true，不能再用作实时判断
      writeBackHasSelection.value = !props.runtimeContext.hasEmptySelection;

      const rawHtml = md.render(entry.assistantText || "");
      writeBackHtml.value = DOMPurify.sanitize(rawHtml, {
        ALLOWED_TAGS: WRITE_BACK_ALLOWED_TAGS,
        ALLOWED_ATTR: WRITE_BACK_ALLOWED_ATTR,
      });

      // 若当前无选区，默认切回光标插入模式
      if (!writeBackHasSelection.value) {
        writeBackMode.value = "cursor";
      }

      writeBackStore.reset();
      writeBackDialogVisible.value = true;
    }
    ```

    **步骤 6：新增 confirmWriteBack() 函数（Pinia store 模式，评审 Issue #1 修复）**
    ```javascript
    function confirmWriteBack() {
      // 设置 loading 状态，由 EditorShell.handleInsertHtml 异步更新为 success/error
      writeBackStore.status = "loading";
      emit("insert-html", {
        html: writeBackHtml.value,
        // 无 onSuccess/onError 回调：结果通过 writeBackStore.status 反馈（method B）
      });
    }
    ```

    **步骤 7：监听 writeBackStore.status 变化**
    ```javascript
    watch(
      () => writeBackStore.status,
      (status) => {
        if (status === "success") {
          writeBackDialogVisible.value = false;
          ElMessage.success("已写入文档");
          writeBackStore.reset();
        } else if (status === "error") {
          // 保持对话框开启，允许用户重试
          ElMessage.error(writeBackStore.errorMsg || "写入文档失败，请重试");
          writeBackStore.reset();
        }
      }
    );
    ```

    **步骤 8：message-actions div 追加「写入文档」按钮（delete 按钮之后）**
    ```html
    <el-button
      size="small" text type="primary"
      v-if="entry.status === 'completed'"
      @click="openWriteBackDialog(entry)"
      title="将回复写入文档"
    >
      <el-icon><Position /></el-icon>
    </el-button>
    ```
    若 Position 图标未导入，在顶部 icons import 解构中追加 Position。

    **步骤 9：在 </template> 前追加确认对话框**
    ```html
    <el-dialog
      v-model="writeBackDialogVisible"
      title="写入文档" width="560px"
      :close-on-click-modal="writeBackStore.status !== 'loading'"
      :close-on-press-escape="writeBackStore.status !== 'loading'"
      append-to-body
    >
      <div class="writeback-preview markdown-body"
           v-html="writeBackHtml"
           style="max-height:320px;overflow-y:auto;border:1px solid var(--el-border-color);border-radius:4px;padding:12px;margin-bottom:16px;">
      </div>

      <!-- 评审 Issue #2 缓解：替换模式下提示选区可能失效 -->
      <el-alert
        v-if="writeBackMode === 'replace' && writeBackHasSelection"
        type="info"
        :closable="false"
        style="margin-bottom:12px;"
        description="注意：确认框打开后文档选区可能因焦点转移而失效。如「替换选区」未能替换预期内容，请关闭此框在文档中重新选中后写入。"
      />

      <el-radio-group v-model="writeBackMode" style="display:flex;flex-direction:column;gap:8px;">
        <el-radio value="cursor">插入到光标</el-radio>
        <el-radio value="replace" :disabled="!writeBackHasSelection">
          替换当前选区
          <span v-if="!writeBackHasSelection"
                style="font-size:12px;color:var(--el-text-color-secondary);margin-left:4px;">（当前无选区）</span>
        </el-radio>
      </el-radio-group>

      <template #footer>
        <el-button @click="writeBackDialogVisible = false" :disabled="writeBackStore.status === 'loading'">取消</el-button>
        <el-button type="primary" @click="confirmWriteBack" :loading="writeBackStore.status === 'loading'">确认写入</el-button>
      </template>
    </el-dialog>
    ```

    **步骤 10：workbench-top-bar 追加保存状态标签**
    在 .top-actions div 内现有按钮之后追加：
    ```html
    <el-tag v-if="props.runtimeContext.saveStatus" size="small"
            :type="saveStatusTagType(props.runtimeContext.saveStatus?.state)"
            style="margin-left:4px;">
      {{ saveStatusLabel(props.runtimeContext.saveStatus?.state) }}
    </el-tag>
    ```
    在 script setup 末尾追加辅助函数：
    ```javascript
    function saveStatusTagType(state) {
      if (!state) return "info";
      if (state === "saved") return "success";
      if (state === "save-failed" || state === "failed") return "danger";
      if (state === "editing" || state === "callback-received") return "warning";
      return "info";
    }
    function saveStatusLabel(state) {
      const map = { idle:"已同步", editing:"编辑中", "callback-received":"保存中",
                    saved:"已保存", "save-failed":"保存失败", failed:"异常" };
      return map[state] ?? state ?? "—";
    }
    ```
  </action>
  <verify>
    <automated>corepack pnpm --dir packages/web test -- src/test/EditorAiWorkbench.test.js --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - dompurify 在 packages/web/package.json dependencies 中 ✓
    - `WRITE_BACK_ALLOWED_TAGS` 和 `WRITE_BACK_ALLOWED_ATTR` 常量在文件中定义 ✓
    - `DOMPurify.sanitize(rawHtml, { ALLOWED_TAGS: WRITE_BACK_ALLOWED_TAGS, ALLOWED_ATTR: WRITE_BACK_ALLOWED_ATTR })` 被调用 ✓
    - `writeBackHasSelection` ref 存在，在 openWriteBackDialog 中赋值为 `!props.runtimeContext.hasEmptySelection` ✓
    - 「替换选区」radio 的 `:disabled` 绑定使用 `!writeBackHasSelection`（非实时 runtimeContext）✓
    - ElAlert 在 `writeBackMode === 'replace' && writeBackHasSelection` 时显示 ✓
    - `confirmWriteBack` 设置 `writeBackStore.status = 'loading'`，emit insert-html 仅含 `{ html }` ✓
    - `watch(writeBackStore.status)` 处理 success/error 路径 ✓
    - 全部现有测试不回退 ✓
  </acceptance_criteria>
  <done>
    - dompurify 在 package.json 依赖中
    - WRITE_BACK_ALLOWED_TAGS/ALLOWED_ATTR 明确白名单
    - writeBackHasSelection 快照冻结逻辑完整
    - ElAlert 替换模式警示存在
    - confirmWriteBack 使用 Pinia store 反馈模式
    - watch 处理 success/error 两条路径
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展 EditorAiWorkbench.test.js 覆盖写回 UI 行为（Pinia store 反馈模式）</name>
  <files>packages/web/src/test/EditorAiWorkbench.test.js</files>
  <read_first>
    - packages/web/src/test/EditorAiWorkbench.test.js
    - packages/web/src/stores/writeBackStore.js
  </read_first>
  <behavior>
    - completed 条目显示「写入文档」按钮
    - 点击后对话框显示（含模式选择）
    - openWriteBackDialog 冻结 writeBackHasSelection（TOCTOU 验证）
    - writeBackHasSelection=false 时「替换当前选区」禁用（无选区场景）
    - confirmWriteBack 设 store.status='loading' 并 emit insert-html（含 html，无回调）
    - store.status='success' → ElMessage.success + 对话框关闭
    - store.status='error' → ElMessage.error + 对话框保持开启
  </behavior>
  <action>
    在测试文件现有测试末尾追加 describe 块：

    ```javascript
    describe("写入文档功能（Pinia store 反馈模式）", () => {
      let writeBackStore;

      beforeEach(() => {
        // 创建独立 Pinia 实例用于测试
        const pinia = createTestingPinia({ createSpy: vi.fn });
        writeBackStore = useWriteBackStore(pinia);
      });

      it("completed 条目显示「写入文档」按钮", async () => {
        const wrapper = mountWorkbench({ runtimeContext: runtimeContext({ hasEmptySelection: false }) });
        await flushPromises();
        await wrapper.find("textarea").setValue("测试");
        await wrapper.find('button[title="发送问题"]').trigger("click");
        await flushPromises();

        expect(wrapper.find('button[title="将回复写入文档"]').exists()).toBe(true);
      });

      it("openWriteBackDialog 冻结 writeBackHasSelection 快照（TOCTOU 修复验证）", async () => {
        // 打开时有选区
        const wrapper = mountWorkbench({ runtimeContext: runtimeContext({ hasEmptySelection: false }) });
        await flushPromises();
        await wrapper.find("textarea").setValue("测试");
        await wrapper.find('button[title="发送问题"]').trigger("click");
        await flushPromises();

        await wrapper.vm.openWriteBackDialog(wrapper.vm.conversationEntries[0]);

        // 冻结值应为 true（有选区）
        expect(wrapper.vm.writeBackHasSelection).toBe(true);
      });

      it("无选区时「替换当前选区」禁用（基于冻结 writeBackHasSelection）", async () => {
        const wrapper = mountWorkbench({ runtimeContext: runtimeContext({ hasEmptySelection: true }) });
        await flushPromises();
        await wrapper.find("textarea").setValue("测试");
        await wrapper.find('button[title="发送问题"]').trigger("click");
        await flushPromises();

        await wrapper.vm.openWriteBackDialog(wrapper.vm.conversationEntries[0]);
        await flushPromises();

        // 无选区时 writeBackHasSelection=false → 禁用
        expect(wrapper.vm.writeBackHasSelection).toBe(false);
        expect(wrapper.text()).toContain("当前无选区");
      });

      it("confirmWriteBack 设 store.status=loading 并 emit insert-html（无回调）", async () => {
        const wrapper = mountWorkbench({ runtimeContext: runtimeContext({ hasEmptySelection: false }) });
        await flushPromises();
        await wrapper.find("textarea").setValue("测试");
        await wrapper.find('button[title="发送问题"]').trigger("click");
        await flushPromises();

        await wrapper.vm.openWriteBackDialog(wrapper.vm.conversationEntries[0]);
        await wrapper.vm.confirmWriteBack();
        await flushPromises();

        // store 应处于 loading 状态
        expect(writeBackStore.status).toBe("loading");

        // emit 应携带 html，不携带回调函数
        const emitted = wrapper.emitted("insert-html");
        expect(emitted).toBeTruthy();
        expect(emitted[0][0]).toMatchObject({ html: expect.any(String) });
        expect(emitted[0][0].onSuccess).toBeUndefined();
        expect(emitted[0][0].onError).toBeUndefined();
      });

      it("store.status=success → ElMessage.success + 对话框关闭", async () => {
        const wrapper = mountWorkbench({ runtimeContext: runtimeContext({ hasEmptySelection: false }) });
        await flushPromises();
        await wrapper.find("textarea").setValue("测试");
        await wrapper.find('button[title="发送问题"]').trigger("click");
        await flushPromises();

        await wrapper.vm.openWriteBackDialog(wrapper.vm.conversationEntries[0]);
        await wrapper.vm.confirmWriteBack();
        await flushPromises();

        // 模拟 EditorShell 更新 store
        writeBackStore.status = "success";
        await flushPromises();

        expect(elementPlusMocks.success).toHaveBeenCalledWith("已写入文档");
        expect(wrapper.vm.writeBackDialogVisible).toBe(false);
      });

      it("store.status=error → ElMessage.error + 对话框保持开启", async () => {
        const wrapper = mountWorkbench({ runtimeContext: runtimeContext({ hasEmptySelection: false }) });
        await flushPromises();
        await wrapper.find("textarea").setValue("测试");
        await wrapper.find('button[title="发送问题"]').trigger("click");
        await flushPromises();

        await wrapper.vm.openWriteBackDialog(wrapper.vm.conversationEntries[0]);
        await wrapper.vm.confirmWriteBack();
        await flushPromises();

        // 模拟 EditorShell 更新 store 为 error
        writeBackStore.status = "error";
        writeBackStore.errorMsg = "写入超时";
        await flushPromises();

        expect(elementPlusMocks.error).toHaveBeenCalledWith("写入超时");
        expect(wrapper.vm.writeBackDialogVisible).toBe(true);
      });
    });
    ```
  </action>
  <verify>
    <automated>corepack pnpm --dir packages/web test -- src/test/EditorAiWorkbench.test.js --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - TOCTOU 冻结快照测试：openWriteBackDialog 后 writeBackHasSelection 值正确 ✓
    - emit insert-html 不包含 onSuccess/onError 回调 ✓
    - store.status=success 触发 ElMessage.success + 对话框关闭 ✓
    - store.status=error 触发 ElMessage.error + 对话框保持开启 ✓
    - 原有全部测试不回退 ✓
  </acceptance_criteria>
  <done>
    - 全部写回测试 PASS
    - TOCTOU 快照、store 反馈两条路径均有测试覆盖
    - 原有测试不回退
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| assistantText → md.render() → DOMPurify(ALLOWED_TAGS) → v-html | AI 模型输出经 markdown-it 转换后 DOMPurify 明确白名单净化，再作为 innerHTML |
| emit insert-html.html → EditorShell → bridge | 净化后 HTML 跨组件传递至 bridge |
| writeBackStore.status | EditorShell 写入，EditorAiWorkbench 监听；Pinia 响应式，无 XSS 风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | Tampering/XSS | writeBackHtml v-html 预览区 | mitigate | DOMPurify.sanitize(rawHtml, { ALLOWED_TAGS: [...], ALLOWED_ATTR: ['href'] }) 明确白名单净化 LLM 输出 |
| T-15-02-02 | Tampering/XSS | insert-html emit.html | mitigate | 同上，净化后的 HTML 才进入 emit payload，ONLYOFFICE PasteHtml 接收净化内容 |
| T-15-02-03 | Denial of Service | writeBackLoading 双重提交 | mitigate | 确认按钮 :loading="writeBackStore.status === 'loading'" 禁止重复点击 |
| T-15-02-04 | Race Condition | TOCTOU 选区失效 | mitigate | openWriteBackDialog 冻结 writeBackHasSelection 快照；ElAlert 警示用户 |
| T-15-02-05 | False-positive UX | success 时序 | mitigate | Pinia store 模式：success 消息在 EditorShell bridge 成功后才触发（非同步 emit 后立即触发）|
</threat_model>

<verification>
```bash
corepack pnpm --dir packages/web test -- src/test/EditorAiWorkbench.test.js --reporter=verbose
```
</verification>

<success_criteria>
- dompurify 已安装（package.json 含此依赖）✓
- `WRITE_BACK_ALLOWED_TAGS` / `WRITE_BACK_ALLOWED_ATTR` 明确白名单常量定义 ✓
- `writeBackStore.js` 存在（status/errorMsg/reset）✓
- `writeBackHasSelection` 在 openWriteBackDialog 中冻结快照（TOCTOU 修复）✓
- ElAlert 在替换模式且有选区时显示选区失效警示 ✓
- `confirmWriteBack` 设置 `writeBackStore.status = 'loading'`，emit 仅含 `{ html }` ✓
- `watch(writeBackStore.status)` 处理 success/error 反馈 ✓
- 全部 EditorAiWorkbench 测试通过 ✓
</success_criteria>

<output>
完成后创建 `.planning/phases/15-ai-writeback/15-02-SUMMARY.md`
</output>
</output>
