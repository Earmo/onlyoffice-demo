---
phase: 05-distributed-editing-flow
plan: 03
type: backend-remote-resource-hardening
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/service/DocumentStorageService.java
  - packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/service/OnlyofficeImageService.java
  - packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/config/OnlyofficeIntegrationProperties.java
  - packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/service/DocumentStorageServiceTest.java
  - packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/service/OnlyofficeImageServiceTest.java
  - docs/minimal-integration.md
autonomous: true
requirements:
  - SAFE-02
  - SAFE-03
user_setup: []
must_haves:
  - 远程导入和图片代理统一纳入 SSRF 防护
  - 文档导入和图片代理都有响应大小上限与媒体类型校验
  - 被安全策略拒绝时返回明确可解释的错误
---

# Plan 05-03: 补齐远程资源安全边界与分布式状态收口

## Outcome

把“这个服务能安全地向外部 URL 发请求吗”这件事做扎实：统一强化远程导入和图片代理的 SSRF、防大包和媒体类型边界，并把新的拒绝语义写回接入文档和验证清单。

## Tasks

<task id="5-03-01">
  <read_first>
    <file>packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/service/DocumentStorageService.java</file>
    <file>packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/service/OnlyofficeImageService.java</file>
    <file>.planning/phases/05-distributed-editing-flow/05-CONTEXT.md</file>
  </read_first>
  <action>
    统一收口远程导入和图片代理的 SSRF 防护。具体要求：
    1. 远程文档导入和图片代理共享一致的 SSRF 策略，而不是两套不一致的校验；
    2. 至少阻止回环、本机、私网保留地址段等明显风险目标；
    3. 被策略拒绝时返回明确 `4xx + 可读错误信息`；
    4. `DocumentStorageServiceTest` 和 `OnlyofficeImageServiceTest` 至少覆盖被阻止地址场景。
  </action>
  <verify>`cd packages/server && mvn -q -DskipITs -Dtest=DocumentStorageServiceTest,OnlyofficeImageServiceTest test`</verify>
  <acceptance_criteria>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/service/DocumentStorageService.java` contains SSRF protection beyond localhost only</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/service/OnlyofficeImageService.java` contains SSRF protection beyond localhost only</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/service/DocumentStorageServiceTest.java` contains blocked target coverage</criterion>
  </acceptance_criteria>
  <done>远程资源入口已经具备统一 SSRF 边界，不再只是阻止 localhost 的最小示例实现。</done>
</task>

<task id="5-03-02">
  <read_first>
    <file>packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/service/DocumentStorageService.java</file>
    <file>packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/service/OnlyofficeImageService.java</file>
    <file>.planning/phases/05-distributed-editing-flow/05-RESEARCH.md</file>
  </read_first>
  <action>
    为远程资源补齐响应大小与媒体类型校验。具体要求：
    1. 文档导入和图片代理都增加显式响应大小上限；
    2. 文档导入执行扩展名 + 内容类型双校验，图片代理校验图片 media type；
    3. 配置项应集中落到 `OnlyofficeIntegrationProperties` 或等价统一配置模型；
    4. `DocumentStorageServiceTest` 与 `OnlyofficeImageServiceTest` 至少覆盖超限或媒体类型非法场景。
  </action>
  <verify>`cd packages/server && mvn -q -DskipITs -Dtest=DocumentStorageServiceTest,OnlyofficeImageServiceTest test`</verify>
  <acceptance_criteria>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/service/DocumentStorageService.java` contains size limit and content-type checks</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/service/OnlyofficeImageService.java` contains image media type checks</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/service/OnlyofficeImageServiceTest.java` contains invalid media type or size-limit coverage</criterion>
  </acceptance_criteria>
  <done>远程导入和图片代理已经具备防大包与媒体类型约束，不再只是“能下载就算成功”。</done>
</task>

<task id="5-03-03">
  <read_first>
    <file>docs/minimal-integration.md</file>
    <file>.planning/phases/05-distributed-editing-flow/05-VALIDATION.md</file>
    <file>.planning/phases/05-distributed-editing-flow/05-CONTEXT.md</file>
  </read_first>
  <action>
    收口分布式编辑阶段的安全说明和验证路径。具体要求：
    1. 最小接入文档补充 callback JWT、运行时地址配置和远程资源安全边界说明；
    2. 手动验证清单明确 callback 拒绝、坏地址配置、远程资源被安全策略拒绝这三类路径；
    3. 本计划完成后全量测试和 `docker compose config` 都应通过；
    4. 文档表述必须体现“官方前端仍只消费 documentId + editor-config”的边界。
  </action>
  <verify>`cd packages/server && mvn test && docker compose config`</verify>
  <acceptance_criteria>
    <criterion>`docs/minimal-integration.md` contains callback JWT or runtime URL safety guidance</criterion>
    <criterion>`.planning/phases/05-distributed-editing-flow/05-VALIDATION.md` contains security rejection verification paths</criterion>
    <criterion>`docs/minimal-integration.md` contains `documentId + editor-config` or equivalent runtime boundary wording</criterion>
  </acceptance_criteria>
  <done>Phase 5 的运行时安全边界与接入说明已经收口成可交付的阶段结果。</done>
</task>

## Verification

- `cd packages/server && mvn -q -DskipITs -Dtest=DocumentStorageServiceTest,OnlyofficeImageServiceTest test`
- `cd packages/server && mvn test`
- `docker compose config`

## Notes for Execution

- 本计划是 Phase 5 的安全收口层，重点是远程资源边界和可解释拒绝，不要在这里顺手扩完整网络治理平台。
- 如果实现中需要抽公共安全校验组件，优先把导入文档和图片代理统一接到同一套约束上。
