---
phase: 06-verification-and-delivery
plan: 03
type: delivery-entrypoints-and-docs
wave: 2
depends_on:
  - 01
  - 02
files_modified:
  - README.md
  - docs/
  - package.json
  - scripts/
  - .planning/phases/06-verification-and-delivery/06-VALIDATION.md
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03
user_setup: []
must_haves:
  - 仓库根具备统一且分层的验证入口
  - 交付文档清晰覆盖独立部署、微服务接入和验收清单
  - 新接手的人无需翻 planning 也能知道怎么验证和交付
---

# Plan 06-03: 统一验证命令并编写独立部署 / 微服务接入说明

## Outcome

把前面已经存在的测试与构建能力收口成真正的交付面：提供根级分层验证命令、README 推荐顺序、交付说明、配置矩阵和简洁验收清单。

## Tasks

<task id="6-03-01">
  <read_first>
    <file>README.md</file>
    <file>packages/server/pom.xml</file>
    <file>packages/web/package.json</file>
    <file>.planning/phases/06-verification-and-delivery/06-CONTEXT.md</file>
  </read_first>
  <action>
    建立根级统一验证入口。具体要求：
    1. 从仓库根提供分层验证命令或脚本，至少覆盖后端测试、前端测试、前端构建、compose 配置校验和总体验证；
    2. 保留子模块原生命令，不用统一入口替代所有底层命令；
    3. 命令名称和输出要便于新接手的人理解，不设计成只能内部人看懂的脚本；
    4. 统一入口必须能被后续文档直接引用。
  </action>
  <verify>`根级统一验证命令（由本计划提供）`</verify>
  <acceptance_criteria>
    <criterion>Repository root contains a layered verification entrypoint or equivalent command set</criterion>
    <criterion>The unified verification entrypoint covers backend tests, frontend tests/build, and compose config checks</criterion>
    <criterion>Sub-package commands remain available and are not removed</criterion>
  </acceptance_criteria>
  <done>仓库根已经具备面向交付的统一验证入口，不再需要手工拼命令。</done>
</task>

<task id="6-03-02">
  <read_first>
    <file>README.md</file>
    <file>docs/minimal-integration.md</file>
    <file>.planning/phases/06-verification-and-delivery/06-RESEARCH.md</file>
  </read_first>
  <action>
    收口交付文档结构。具体要求：
    1. README 作为总入口，明确推荐执行顺序和文档导航；
    2. docs 结构至少能清晰承载独立部署说明、微服务接入说明、配置矩阵和简洁验收清单；
    3. `minimal-integration.md` 保持“最小接入说明”定位，不把所有交付内容都塞进去；
    4. 文档内容必须能支撑独立部署和作为微服务接入这两条主线。
  </action>
  <verify>`docker compose config`</verify>
  <acceptance_criteria>
    <criterion>`README.md` contains recommended verification order and documentation navigation</criterion>
    <criterion>`docs/` contains explicit delivery-facing documentation beyond only `minimal-integration.md`</criterion>
    <criterion>Documentation clearly separates minimal integration from fuller delivery guidance</criterion>
  </acceptance_criteria>
  <done>交付文档已经形成清晰结构，仓库入口和 docs 子文档各司其职。</done>
</task>

<task id="6-03-03">
  <read_first>
    <file>README.md</file>
    <file>docs/minimal-integration.md</file>
    <file>.planning/phases/06-verification-and-delivery/06-VALIDATION.md</file>
  </read_first>
  <action>
    收口交付验证与手动验收路径。具体要求：
    1. 提供一份简洁的人工验收/交付验证清单，承接自动化之外的关键检查点；
    2. 更新 `06-VALIDATION.md` 中与统一入口相关的 future command 说明，使其和实际落地入口一致；
    3. 完成后，统一验证入口、后端测试、前端测试/构建和 compose 校验应都能串联起来；
    4. 最终输出必须让新接手的人无需翻 `.planning` 也能完成一次完整验证。
  </action>
  <verify>`根级统一验证命令（由本计划提供）`</verify>
  <acceptance_criteria>
    <criterion>Delivery-facing documentation includes a concise manual acceptance checklist</criterion>
    <criterion>`.planning/phases/06-verification-and-delivery/06-VALIDATION.md` references the actual unified verification entrypoint</criterion>
    <criterion>The repository can be verified end-to-end from the root using the documented command flow</criterion>
  </acceptance_criteria>
  <done>Phase 6 的验证入口和交付说明已经闭环成真正可执行的服务基线。</done>
</task>

## Verification

- `根级统一验证命令（由本计划提供）`
- `docker compose config`
- `cd packages/server && mvn test`
- `cd packages/web && corepack pnpm test -- --run`
- `cd packages/web && corepack pnpm build`

## Notes for Execution

- 本计划的重点是交付入口和文档结构收口，不要顺手扩成完整运维手册工程。
- 统一验证入口要服务“理解和执行”，而不是追求技巧性脚本设计。
