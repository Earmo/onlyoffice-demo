---
phase: 02-storage-strategy-layer
plan: 03
type: backend-storage-governance
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - packages/server/onlyoffice-integration-service/src/main/resources/application.yml
  - packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/storage/StorageProviderResolver.java
  - packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/model/DocumentSummaryResponse.java
  - packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/web/DocumentApiController.java
  - packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/storage/StorageProviderResolverTest.java
  - packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/web/DocumentApiControllerTest.java
  - docs/minimal-integration.md
  - docker-compose.yml
autonomous: true
requirements:
  - STOR-02
  - STOR-03
user_setup: []
must_haves:
  - provider 选择规则已经标准化，可默认按 profile 工作，并预留按 `tenant/sourceSystem` 扩展
  - 文档列表和详情对对象缺失具备异常可见性，而不是静默隐藏异常文档
  - 文档、配置和 compose 联调说明已经把 MinIO 默认基线、local 开发回退和未来 COS/OSS 扩展约定写清楚
---

# Plan 02-03: 固化多存储策略的配置和扩展约定

## Outcome

把 provider 路由规则、对象缺失可见性、MinIO 默认基线和未来 COS / OSS 扩展约定收口到稳定配置与接入文档里。

## Tasks

<task id="2-03-01">
  <read_first>
    <file>packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/storage/StorageProviderResolver.java</file>
    <file>packages/server/onlyoffice-integration-service/src/main/resources/application.yml</file>
    <file>.planning/phases/02-storage-strategy-layer/02-CONTEXT.md</file>
    <file>.planning/phases/02-storage-strategy-layer/02-RESEARCH.md</file>
  </read_first>
  <action>
    固化 provider 路由约定，并让默认行为与 profile 对齐。具体要求：
    `application.yml` 中把 `default-provider` 默认值固定为 `local`，并通过 `docker-compose.yml` 的 `ONLYOFFICE_INTEGRATION_STORAGE_DEFAULT_PROVIDER=minio` 环境变量把 compose / 正式联调默认 provider 覆盖成 `minio`；
    `StorageProviderResolver` 至少按以下优先级解析 provider：
    `tenant 路由 > sourceSystem 路由 > defaultProvider`；
    当找不到映射时，始终回退到 `defaultProvider`，禁止抛出空 provider；
    在代码和注释中明确说明 COS / OSS 后续只需要新增 provider 实现和配置映射，不需要改 controller 或 `DocumentStorageService` 的上层调用签名。
  </action>
  <verify>`cd packages/server && mvn -q -DskipITs -Dtest=StorageProviderResolverTest test`</verify>
  <acceptance_criteria>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/storage/StorageProviderResolver.java` contains `tenant`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/storage/StorageProviderResolver.java` contains `sourceSystem`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/storage/StorageProviderResolver.java` contains `defaultProvider`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/storage/StorageProviderResolverTest.java` exists</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/resources/application.yml` contains `source-systems`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/resources/application.yml` contains `tenants`</criterion>
  </acceptance_criteria>
  <done>provider 路由优先级和默认行为已经稳定，后续扩 COS / OSS 不需要改上层业务调用链。</done>
</task>

<task id="2-03-02">
  <read_first>
    <file>packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/web/DocumentApiController.java</file>
    <file>packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/model/DocumentSummaryResponse.java</file>
    <file>packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/service/DocumentStorageService.java</file>
    <file>.planning/phases/02-storage-strategy-layer/02-CONTEXT.md</file>
  </read_first>
  <action>
    把“元数据存在但对象缺失”的产品语义体现在 API 投影里。具体要求：
    `DocumentSummaryResponse` 必须新增布尔字段 `storageAvailable`；
    `DocumentApiController.list` 和 `detail` 在返回文档摘要时，必须基于当前 provider 调用统一存储接口做存在性检查；
    当对象缺失时，不得隐藏文档记录；响应中必须能让调用方看出该文档异常，且如果当前状态不是 `failed`，允许在投影层额外给出 `storageAvailable=false`；
    打开文件或 editor-config 的路径保持明确报错，不在这一层做静默自动修复。
  </action>
  <verify>`cd packages/server && mvn -q -DskipITs -Dtest=DocumentApiControllerTest test`</verify>
  <acceptance_criteria>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/model/DocumentSummaryResponse.java` contains `storageAvailable`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/web/DocumentApiController.java` contains `storageAvailable`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/java/com/earmo/onlyoffice/integration/web/DocumentApiController.java` contains `exists(`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/test/java/com/earmo/onlyoffice/integration/web/DocumentApiControllerTest.java` contains `storageAvailable`</criterion>
    <criterion>`cd packages/server && mvn -q -DskipITs -Dtest=DocumentApiControllerTest test` exits 0</criterion>
  </acceptance_criteria>
  <done>异常文档不会从列表中消失，API 已能显式表达对象可用性。</done>
</task>

<task id="2-03-03">
  <read_first>
    <file>docs/minimal-integration.md</file>
    <file>docker-compose.yml</file>
    <file>packages/server/onlyoffice-integration-service/src/main/resources/application.yml</file>
    <file>.planning/phases/02-storage-strategy-layer/02-CONTEXT.md</file>
  </read_first>
  <action>
    更新接入文档和联调编排，把多存储策略约定一次性写清。文档中至少要明确：
    1. 正式基线 provider 是 `minio`，`local` 只作为 `dev/test` 或过渡回退；
    2. 对象键固定为 `tenant/sourceSystem/documentId.ext`；
    3. 创建、上传、导入失败时尽量回滚，不留下半成品文档；
    4. callback 回写失败时保留旧版本并将状态标记为 `failed`；
    5. 将来接入 COS / OSS 只需要新增 provider 实现与配置，不需要改上层 API 流程。
    同时确保 `docker-compose.yml` 和 `application.yml` 的默认值与文档表述一致，避免一边写 MinIO 默认、一边仍在 compose 中默认 local。
  </action>
  <verify>`docker compose config`</verify>
  <acceptance_criteria>
    <criterion>`docs/minimal-integration.md` contains `minio`</criterion>
    <criterion>`docs/minimal-integration.md` contains `local`</criterion>
    <criterion>`docs/minimal-integration.md` contains `tenant/sourceSystem/documentId.ext`</criterion>
    <criterion>`docs/minimal-integration.md` contains `COS`</criterion>
    <criterion>`docs/minimal-integration.md` contains `OSS`</criterion>
    <criterion>`docker-compose.yml` contains `ONLYOFFICE_INTEGRATION_STORAGE_DEFAULT_PROVIDER`</criterion>
    <criterion>`packages/server/onlyoffice-integration-service/src/main/resources/application.yml` contains `default-provider`</criterion>
  </acceptance_criteria>
  <done>MinIO 默认基线、local 回退路径和未来 provider 扩展约定已经在配置与文档中收口一致。</done>
</task>

## Verification

- `cd packages/server && mvn -q -DskipITs test`
- `cd packages/server && mvn test`
- `docker compose config`

## Notes for Execution

- 本计划是 Phase 2 的收口层，重点是把 provider 规则、异常可见性和接入文档讲清楚，而不是继续新增一套更大的文件平台。
- 若执行中发现列表投影需要新增 DTO 字段，优先扩展现有响应对象，不要让前端自己拼接对象存储状态。
