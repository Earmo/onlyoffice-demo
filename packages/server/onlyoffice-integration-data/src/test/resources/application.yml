spring:
  datasource:
    url: jdbc:h2:mem:onlyoffice-integration-data-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  flyway:
    enabled: true

mybatis-flex:
  configuration:
    map-underscore-to-camel-case: true
